"""The /v1 REST surface: the Decision object as the versioned public contract, plus an
override endpoint, serving both on-demand and batch invocation from one engine.

Boundary stub — populated in Phase 2.
"""

__all__: list[str] = []
