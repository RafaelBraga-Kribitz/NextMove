---
phase: 01-reproducible-world
plan: 11
type: execute
wave: 7
depends_on: ["01-02", "01-04", "01-08", "01-09", "01-10"]
files_modified:
  - dvc.yaml
  - justfile
  - .github/workflows/ci.yml
  - src/nextmove/storage/__main__.py
  - docs/SIMULATOR_ASSUMPTIONS.md
  - tests/golden/test_reproducibility.py
  - tests/integration/test_config_drives_behavior.py
  - tests/unit/test_simulator_assumptions_doc.py
autonomous: true
requirements: [ENG-04, ENG-08, ENG-09, SIM-03]

must_haves:
  truths:
    - "One command on a clean laptop regenerates the full simulated history into canonical event tables and point-in-time feature tables, byte-identical across two seeded runs, with no cloud dependency (phase success criterion 1, ENG-04, ENG-08)"
    - "make reproduce forwards to just reproduce, which runs the DVC pipeline stages simulate, ingest and features in order (D-14, ENG-08)"
    - "Every derived table's lineage chain is printable from the command line, so a reviewer can read the content hashes of its inputs and the config hash that produced it (DATA-04, ENG-09)"
    - "A reviewer can open docs/SIMULATOR_ASSUMPTIONS.md and find every generative assumption written down — the latent trait distributions and their parameters, the seasonality curve, the response-function form, the fatigue penalty and its config toggle, the micro-event rates and dwell-class boundaries (SIM-03, phase success criterion 5)"
    - "Changing the seasonality curve, the feature list, or a simulator parameter in YAML changes the generated output with no code edit (ENG-03 end to end, phase success criterion 5)"
    - "Two ci-profile runs whose seeds are equal produce byte-identical Parquet, and two runs differing only in the seed produce different bytes — equal seeds never diverge and different seeds never collide (ENG-04, edge: adjacency)"
    - "A pipeline stage that receives zero input rows writes a zero-row Parquet file carrying the full schema rather than a missing file, and that file is byte-identical across runs (ENG-04, edge: empty)"
    - "An interrupted just reproduce leaves no partially-written table in the output directory; each stage writes to a temporary path and atomically renames on success (ENG-08, edge: concurrency)"
    - "Every path written by a stage is declared as an out of that stage and of no other; no stage declares one of its own outs as one of its own deps; and no lineage fragment is any stage's dep — while the ordinary producer-to-consumer edges that wire the DAG together, simulate's data/raw/ becoming ingest's dependency and ingest's data/canonical/ becoming features', remain intact and are required to be present. So no stage writes into a path a sibling owns, and dvc repro never reports a stage stale because a sibling touched its directory (ENG-09, review HIGH-3, review HIGH-7)"
    - "Re-running the pipeline with unchanged inputs and config performs no work and leaves dvc.lock byte-identical, and re-recording lineage for an existing table upserts rather than appending a duplicate row (ENG-09, edge: idempotency)"
    - "SIMULATOR_ASSUMPTIONS.md contains at least one entry per generative parameter group, and the doc-floor test fails when the file is empty or contains only headings (SIM-03, edge: empty)"
    - statement: "Two generative assumptions describing adjacent parameter ranges are documented as distinct entries with explicit non-overlapping ranges, never merged into one ambiguous range"
      verification: backstop
    - statement: "When two assumption entries belong to the same parameter group, their order in SIMULATOR_ASSUMPTIONS.md is fixed by the document's declared section order and is stable across edits"
      verification: backstop
    - statement: "Two concurrent pipeline runs against the same workspace do not interleave writes into the same table — the run either serializes on the workspace lock or fails loudly; it never produces a half-merged table"
      verification: backstop
  artifacts:
    - path: "dvc.yaml"
      provides: "the three pipeline stages simulate, ingest, features with their declared deps and non-overlapping outs, including one lineage fragment per stage"
      contains: "stages"
    - path: "docs/SIMULATOR_ASSUMPTIONS.md"
      provides: "every generative assumption in the simulator, written for a reviewer"
      min_lines: 120
    - path: "tests/golden/test_reproducibility.py"
      provides: "byte-identical two-run proof over the full ci-profile pipeline"
      min_lines: 70
    - path: "tests/integration/test_config_drives_behavior.py"
      provides: "end-to-end proof that a YAML edit changes output with no code edit"
      min_lines: 50
    - path: "src/nextmove/storage/__main__.py"
      provides: "CLI printing a table's lineage chain"
  key_links:
    - from: "Makefile"
      to: "justfile"
      via: "make reproduce forwards to just reproduce"
      pattern: "just reproduce"
    - from: "justfile"
      to: "dvc.yaml"
      via: "the reproduce recipe invokes dvc repro over the three declared stages"
      pattern: "dvc repro"
    - from: "docs/SIMULATOR_ASSUMPTIONS.md"
      to: "config/simulator.yaml"
      via: "every documented parameter names the config key it is read from"
      pattern: "config/simulator.yaml"
    - from: ".github/workflows/ci.yml"
      to: "tests/golden/test_reproducibility.py"
      via: "the CI job runs the miniature end-to-end and the golden comparison"
      pattern: "golden"
  prohibitions:
    - statement: "MUST NOT tune simulator parameters, seeds, or the reject-rate threshold in response to a downstream result being unflattering — the parameterization is fixed by documented assumptions, never back-fitted so a later phase's numbers look better"
      status: resolved
      verification: judgment
    - statement: "MUST NOT silently regenerate or overwrite a golden fixture to make a failing determinism or leakage test pass; a golden update must be an explicit, separately-committed change with its cause stated"
      status: resolved
      verification: judgment
---

<objective>
Close the phase: wire the three DVC pipeline stages, make `just reproduce` (and therefore
`make reproduce`) the one command that regenerates the whole simulated history, prove
byte-identical reruns with a golden test, prove a YAML edit changes behaviour end to end, extend CI
with the miniature run and the golden comparison, and write `docs/SIMULATOR_ASSUMPTIONS.md`.

Purpose: phase success criteria 1 and 5. Everything before this plan built the pieces; this plan is
where "one command on a clean laptop regenerates the full simulated history, byte-identical across
two seeded runs, with no cloud dependency" becomes a checkable claim rather than an intention.
Output: a reviewable, reproducible, documented phase.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-reproducible-world/01-CONTEXT.md
@.planning/phases/01-reproducible-world/01-RESEARCH.md
</context>

<planner_notes>
**Prohibition-probe canon referrals (recalled, then dropped, recorded for audit).** Three candidates
surfaced during recall are canon security or compliance concerns owned by dedicated tooling and were
therefore not minted as bespoke prohibitions: unsafe YAML deserialization and arbitrary object
construction from a config file (canon — covered by `/gsd-secure-phase` and by threat T-01-05 in plan
01-03); path traversal in artifact and output paths (canon — covered by `/gsd-secure-phase` and by
threat T-01-15 in plan 01-06); personal-data and GDPR exposure in generated tables (canon — covered
by `/gsd-secure-phase`, and additionally out of scope by project non-goal 9, synthetic data only).
</planner_notes>

<tasks>

<task type="auto">
  <name>Task 1: Wire the DVC pipeline stages and the reproduce interface</name>
  <files>dvc.yaml, justfile, src/nextmove/storage/__main__.py</files>
  <read_first>
    - `justfile` — read it before editing; five plans have added to it and the accumulated recipe
      set is not what any single plan's prose implies. Plan 01-01 created **six** recipes: `default`,
      `setup`, `fmt`, `lint`, `test` and a `reproduce` whose body is a placeholder that echoes a
      not-yet-wired message and exits non-zero. Plan 01-04 added `ci` and extended `lint` with
      `lint-imports` (seven). Plan 01-08 added **two**, `simulate` and `budget` — the on-demand
      `default`-profile memory and wall-clock check (nine); plans 01-09 and 01-10 added `ingest` and
      `features` respectively (eleven). That is **eleven** recipes before this task runs, counting
      `default`, which `just --list` does display. This task replaces the `reproduce` body, widens the
      `budget` body (see below), and appends `lineage` and `clean`, bringing the total to
      **thirteen** — it must not redefine any other recipe another plan owns
    - `Makefile` — written in plan 01-01; exactly three lines forwarding `make reproduce` to
      `just reproduce` (D-14)
    - `.dvc/config` and `.dvcignore` — written in plan 01-06 (DVC initialized, no remote, telemetry
      off, `data/_staging/` ignored)
    - `src/nextmove/simulator/run.py`, `src/nextmove/ingest/__main__.py`,
      `src/nextmove/features/__main__.py` — the three stage entry points and the exact table paths
      each writes
    - `src/nextmove/storage/paths.py` — written in plan 01-06; the `Stage` enum, whose three members
      are simultaneously the DVC stage names declared here and the lineage fragment filenames, and
      the `Zone.lineage` member resolving to `data/lineage/`
    - `src/nextmove/storage/lineage.py` — `print_lineage_chain`'s signature, and the per-stage
      fragment layout that makes each stage's lineage output separately declarable
    - `.planning/phases/01-reproducible-world/01-06-SUMMARY.md` — the resolved `Stage` member list
      and the three fragment paths as actually implemented; these must match the stage names declared
      here exactly
    - `.planning/phases/01-reproducible-world/01-CONTEXT.md` — D-14 (`just` is the real operational
      interface; the 3-line Makefile keeps "`make reproduce` from a clean clone" literally true for
      Linux and macOS reviewers), D-18 (Parquet tables tracked by DVC, queried directly by DuckDB)
    - `.planning/phases/01-reproducible-world/01-RESEARCH.md` — the recommended project structure's
      `dvc.yaml` line (stages simulate, ingest, features) and the DVC entry in "Don't Hand-Roll"
      (DVC already solves content-addressable storage and `dvc.lock` staleness detection)
    - `.planning/REQUIREMENTS.md` — ENG-08 verbatim (runs entirely on a laptop; one deployable API
      container; a Makefile/justfile as the operational interface; no orchestration platform, no
      cloud dependency, no streaming infrastructure)
  </read_first>
  <action>
    Create `dvc.yaml` declaring three stages in dependency order. The stage names must be exactly the
    three `Stage` members from `src/nextmove/storage/paths.py` — `simulate`, `ingest`, `features` —
    because each stage's lineage fragment is named for its `Stage` member, and a stage name that
    drifts from an enum member would produce a fragment no stage declares.

    `simulate` — command invoking the simulator module with the profile; deps `config/`,
    `src/nextmove/simulator/`, `src/nextmove/config/`, `src/nextmove/storage/`; outs `data/raw/`,
    `data/ground_truth/`, `data/lineage/simulate.parquet`.
    `ingest` — command invoking the ingest module; deps `data/raw/`, `src/nextmove/ingest/`,
    `src/nextmove/storage/`, `src/nextmove/config/`, `config/data_quality.yaml`; outs
    `data/canonical/`, `data/lineage/ingest.parquet`.
    `features` — command invoking the features module; deps `data/canonical/`,
    `src/nextmove/features/`, `src/nextmove/storage/`, `src/nextmove/config/`,
    `config/features.yaml`; outs `data/features/`, `data/lineage/features.parquet`.

    All three stages depend on `src/nextmove/storage/` and `src/nextmove/config/`, not only
    `simulate`. Most storage changes cascade anyway because they alter `data/raw/` and DVC then
    invalidates downstream, but the uncovered case is a change confined to a code path the simulator
    does not exercise — and there are two of them inside this very phase. `write_table_from_parts`'s
    `dedupe_on` tie-break rule is used only by `ingest`: changing which row survives a duplicate group
    changes `data/canonical/events.parquet` while leaving `data/raw/` byte-identical, so without this
    dep `dvc repro` would report `ingest` up to date over a stale canonical table. `query`'s
    `PRAGMA threads=1` and `STORAGE_MEMORY_LIMIT_MB` are the analogous case for `features`. That is a
    staleness hole in the plan whose entire purpose is ENG-09 staleness detection, and the idempotency
    criteria below would have stayed green straight through it.

    Adding these four deps changes none of the four rules the acceptance criterion below checks:
    `src/nextmove/storage/` and `src/nextmove/config/` are nobody's `outs`, so outs-vs-outs
    disjointness is untouched, no stage gains a dep on one of its own outs, neither path is under
    `data/lineage/`, and both required producer→consumer edges remain exactly as they were. Two stages
    sharing a dep is ordinary and is not what the non-overlap rule forbids.

    Every path a stage writes is declared as an out of that stage and of no other. Do not declare
    `data/lineage/` as a directory out anywhere, and do not declare any stage's lineage fragment as a
    dep of any stage: the whole point of the per-stage fragment layout plan 01-06 established is
    that lineage never appears on both sides of the graph. Do not add `data/_staging/` to any stage —
    it is transient within a run, is deleted before the stage exits, and is already covered by
    `.dvcignore`.

    Be precise about what "no overlap" forbids, because the obvious over-generalization is
    unsatisfiable. What is forbidden is two stages writing the same path, and a stage depending on a
    path it writes. What is *required* is the opposite pattern: one stage's `outs` becoming a
    different stage's `deps` is exactly how a DVC DAG is wired, and it is why `simulate` running
    invalidates `ingest`. `simulate.outs data/raw/` is `ingest.deps data/raw/`, and
    `ingest.outs data/canonical/` is `features.deps data/canonical/`. A check that treated those two
    edges as overlap would fail on every correct pipeline including this one, so the acceptance
    criterion below states the rules separately and additionally requires both edges to be present.

    Declare the stage outs without a `persist` flag. That means `dvc repro` removes a stage's outs
    before re-executing it, so the `ingest` stage always rebuilds `data/canonical/` in full from
    `data/raw/` rather than appending to a surviving table. That is deliberate and is the semantics
    plan 01-09's module docstring records: the append-only path serves direct `just ingest`
    invocation, the pipeline path rematerializes. The two are byte-equal for identical input, and the
    acceptance criteria below force a re-execution and compare digests rather than leaving the claim
    untested — which is what makes a persistent out unnecessary. Do not add one: a persistent out
    would put the canonical zone outside DVC's staleness detection and reopen the reproducibility
    hole this plan exists to close.

    This ownership rule is load-bearing rather than tidy. An earlier draft of this phase put the
    lineage table inside `data/canonical/`, which is `ingest`'s output directory; because every table
    write records lineage, `simulate` and `features` both mutated it. DVC rejects overlapping stage
    outputs, and even where it tolerated the write, `ingest` re-materializing its output directory
    would have destroyed the rows `simulate` recorded — and `dvc.lock` would have changed on every
    run, making this task's byte-identical criterion unsatisfiable and phase success criterion 1
    undemonstrable.

    Parameterize the profile through a DVC variable so `ci`, `demo`, `tiny` and `default` all run the
    same stage definitions. Do not declare a remote — ENG-08 forbids a cloud dependency, and DVC's
    local cache is sufficient for the laptop-only guarantee.

    Replace the placeholder body of the `reproduce` recipe in `justfile` with a `dvc repro`
    invocation over the three stages, taking an optional profile argument that defaults to
    `default`. Confirm `make reproduce` still forwards correctly and that the Makefile is still
    exactly three lines.

    Widen the body of the `budget` recipe — and only its body — so it runs all three budget suites:
    `tests/integration/test_simulation_budget.py`, `tests/integration/test_ingest_budget.py` and
    `tests/integration/test_features_budget.py`, with the gating environment variable set. Plan 01-08
    created the recipe when only the simulator suite existed and recorded that this plan would widen
    it once the other two landed in later waves. One command should cover the whole ENG-08 story
    rather than a third of it: a reviewer who runs `just budget` and sees only the simulator measured
    would reasonably conclude the other two stages were never checked at scale, which was in fact the
    case before this cycle. Do not add a fourth recipe for the other two suites — the recipe count is
    thirteen and this plan's inventory criterion pins it.

    Leave every other recipe's body alone — `default`, `setup`, `fmt`, `lint`, `test`, `ci`,
    `simulate`, `ingest` and `features` are owned by plans 01-01, 01-04, 01-08, 01-09 and 01-10.

    The DVC stages deliberately do not pass `--out` and therefore write to the storage layer's
    default `DATA_ROOT`, which is what makes the repo-relative `outs` paths declared above correct.
    The `--out` flag exists for the golden suite in Task 2, which needs two independent output trees
    from one profile; it is not a pipeline concern.

    Add two more recipes: `lineage table` invoking a new `python -m nextmove.storage` CLI, and
    `clean` removing `data/` without touching the DVC cache.

    Create `src/nextmove/storage/__main__.py` exposing a CLI taking a table name and an optional
    `--root`, printing that table's lineage chain via `print_lineage_chain` with the root forwarded,
    so DATA-04's "the full lineage chain is printable" is a command a reviewer can actually run
    rather than a property of an internal function — and so a chain can be inspected in an alternate
    output tree, which is where the golden suite's second run lands.
  </action>
  <verify>
    <automated>uv run dvc repro --dry 1>/dev/null && just reproduce profile="tiny" && uv run python -m nextmove.storage feature_grid | grep -q feature_grid && test "$(grep -vc '^$' Makefile)" = "3"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run dvc repro --dry` exits 0 and lists exactly the three stages `simulate`, `ingest`, `features`
    - The set of stage names in `dvc.yaml` equals the set of `Stage` member values, asserted by `uv run python -c "import yaml;from nextmove.storage.paths import Stage;d=yaml.safe_load(open('dvc.yaml'));assert set(d['stages'])=={s.value for s in Stage}, (set(d['stages']),{s.value for s in Stage});print('ok')"`
    - **(HIGH-3 / HIGH-7, no stage-output overlap, producer→consumer edges permitted)** A script loads `dvc.yaml`, normalizes every declared path, and asserts exactly four rules, failing with the stage names and the offending path: **(a)** no path is equal to or a parent of a path declared in a *different* stage's `outs` — outs-vs-outs disjointness, which is the property DVC itself rejects a pipeline for violating; **(b)** no stage declares in its own `deps` a path equal to or under one of its own `outs` — a stage may not depend on what it writes; **(c)** no path under `data/lineage/` appears in any stage's `deps` — lineage fragments are outputs only and never appear on both sides of the graph, which is the property review HIGH-3 actually required; **(d)** at least two producer→consumer edges exist, where one stage's `outs` path equals a *different* stage's `deps` path. Rule (d) is deliberately a positive requirement: `simulate.outs data/raw/` is `ingest.deps data/raw/` and `ingest.outs data/canonical/` is `features.deps data/canonical/`, and those edges *are* the pipeline — a check that forbade them could never go green on any correct DVC DAG, and requiring them means the other three rules cannot be satisfied by accidentally declaring a disconnected graph
    - **(HIGH-3)** `data/lineage/` contains exactly three files after a full run — `simulate.parquet`, `ingest.parquet`, `features.parquet` — and each is declared as an out of exactly one stage
    - **(HIGH-3)** After `just reproduce profile="tiny"`, `data/canonical/` contains exactly `events.parquet` and `rejects.parquet` and no lineage file
    - `just reproduce profile="tiny"` exits 0 and produces `data/raw/`, `data/canonical/`, `data/features/` and `data/lineage/`
    - `make reproduce` invokes `just reproduce` and the Makefile still has exactly 3 non-blank lines
    - `dvc.yaml` declares no remote and `grep -c 'remote' dvc.yaml` outputs `0`
    - `uv run python -m nextmove.storage feature_grid` prints a chain naming `feature_grid`, `events` and `events_raw`, proving the chain resolves across all three lineage fragments
    - `just lineage feature_grid` exits 0
    - **(HIGH-3 consequence)** Running `just reproduce profile="tiny"` a second time with nothing changed reports all stages up to date, runs no stage command, and leaves `dvc.lock` byte-identical (compare sha256 before and after)
    - **(HIGH-3 consequence)** Running `just reproduce profile="tiny"` a third time still leaves `dvc.lock` byte-identical, so the no-op property is stable rather than true only on the first repeat
    - **(MEDIUM-5, rematerialization is byte-equal to append)** After a completed `just reproduce profile="tiny"`, record the sha256 of `data/canonical/events.parquet` and `data/canonical/rejects.parquet`, run `uv run dvc repro --force` so all three stages genuinely re-execute rather than reporting up to date, and assert both digests are unchanged. This tests the append-only invariant in the mode that actually ships: under the pipeline DVC removes `data/canonical/` before `ingest` runs, so the run is a full rebuild from `data/raw/`, and this criterion is what proves the rebuild and the append path agree on bytes
    - **(MEDIUM-5)** `grep -c 'persist' dvc.yaml` outputs `0` — no stage out is declared persistent, so DVC's staleness detection covers the canonical zone
    - `just clean` removes `data/` and leaves `.dvc/cache` intact
    - **(LOW-4 / LOW-5)** `just --list` lists exactly thirteen recipes — `default`, `setup`, `fmt`, `lint`, `test`, `ci`, `simulate`, `budget`, `ingest`, `features`, `reproduce`, `lineage` and `clean`. `default` is included because plan 01-01 declares it as a public first recipe and `just --list` displays it; an inventory that omits it disagrees with the tool. This task added only `lineage` and `clean`, and rewrote only the `reproduce` and `budget` bodies
    - **(MEDIUM-9, staleness covers the storage and config packages)** A script asserts every one of the three stages declares both `src/nextmove/storage/` and `src/nextmove/config/` among its `deps`, and a test changes a file under `src/nextmove/storage/` and asserts `dvc status` reports **all three** stages stale rather than only `simulate`
    - **(MEDIUM-9, the four rules still hold)** The four-rule non-overlap script above is re-run after the added deps and still passes, confirming that widening deps did not disturb outs-vs-outs disjointness, the own-out rule, the lineage rule, or the two required producer→consumer edges
  </acceptance_criteria>
  <done>One command regenerates the whole history; every written path is owned by exactly one stage; the lineage chain is printable from the shell; re-running is a genuine no-op with a byte-identical lock file.</done>
</task>

<task type="auto">
  <name>Task 2: Prove byte-identical reruns and config-driven behaviour, and extend CI</name>
  <files>tests/golden/test_reproducibility.py, tests/integration/test_config_drives_behavior.py, .github/workflows/ci.yml</files>
  <read_first>
    - `dvc.yaml` and `justfile` — written in Task 1
    - `tests/golden/test_deterministic_write.py` — written in plan 01-06; this suite extends its
      storage-layer proof to the full pipeline, so reuse its hashing helper rather than duplicating it
    - `.github/workflows/ci.yml` — written in plan 01-04, which left a trailing comment naming this
      plan as the owner of the end-to-end and golden-comparison steps; append to that job, do not
      rewrite the file
    - `.planning/phases/01-reproducible-world/01-RESEARCH.md` — the "Golden-file determinism test
      (ENG-04)" code example, the four determinism sources named in the Summary (randomized string
      hashing, the legacy global NumPy RNG, multi-threaded DuckDB float aggregation, and the Parquet
      writer's own metadata), and the Validation Architecture row mapping ENG-04 to
      `tests/golden/test_reproducibility.py`
    - `.planning/phases/01-reproducible-world/01-CONTEXT.md` — D-16 (CI runs lint, then tests, then a
      miniature end-to-end on the demo profile, then a golden-file comparison), D-25 (seeds are
      config values, so a determinism claim is auditable from the config hash alone)
    - `.planning/REQUIREMENTS.md` — ENG-04 verbatim
  </read_first>
  <action>
    Create `tests/golden/test_reproducibility.py`, marked `slow`, running the `ci` profile twice into
    two separate output roots and asserting that the sha256 of every produced Parquet file matches
    across the two runs. That is twelve files: the nine data tables `events_raw`, `catalog`,
    `inventory_snapshots`, `campaigns`, `events`, `rejects`, `feature_grid`, `customer_traits` and
    `ground_truth_uplift`, plus the three lineage fragments `lineage/simulate.parquet`,
    `lineage/ingest.parquet` and `lineage/features.parquet`, each relative to its own run's root.
    Discover the file set by walking the output root rather than hardcoding it, and additionally
    assert the discovered set equals the expected twelve — a hardcoded list silently stops covering a
    table someone adds, and a bare walk silently passes when a stage writes nothing. Report which
    file diverged on failure; "not byte-identical" without a name is unactionable.

    Obtain the two roots by passing `--out` to each of the three stage modules in turn — `python -m
    nextmove.simulator --profile ci --out <root>`, then `nextmove.ingest`, then `nextmove.features`,
    each against the same `<root>` — and repeat the sequence for a second `tmp_path` root. Do not
    invoke `just reproduce` here: `dvc repro` writes to the default `DATA_ROOT` by design, so two
    pipeline invocations would land in the same tree and the second would overwrite the first,
    making the comparison vacuous. Running the three modules directly is what makes two independent
    output trees obtainable, and it is why plan 01-06 plumbs `root` through the whole storage surface
    and plans 01-08, 01-09 and 01-10 forward `out_root` to it. Assert additionally that neither root
    contains a file belonging to the other and that the repository's own `data/` directory is
    untouched by the test, so a stage that silently ignored its root fails here rather than passing
    by writing both runs to the same path.

    Assert both runs' `_staging` directories are absent at the end, and that the twelve discovered
    files contain no path under `_staging` — a leaked part file would otherwise inflate the discovered
    set and fail the count assertion for the wrong reason.

    The lineage fragments are included in the byte-comparison deliberately. They carry the content
    hashes the whole reproducibility claim rests on, and they are the artifact most likely to acquire
    a wall-clock field or a nondeterministic row order during a later change.

    Add four further assertions in the same file.

    Adjacency — run the `ci` profile twice with the same seed and assert identical digests, then once
    more with one seed value changed and assert at least one digest differs. Equal seeds must never
    diverge and different seeds must never collide; asserting only the first half would pass for a
    pipeline that ignores its seeds entirely.

    Empty — run a profile variant whose horizon produces zero events for at least one table, and
    assert the table file exists, carries the full expected Arrow schema, has zero rows, and is
    byte-identical across two runs. A stage that skips writing on empty input turns a missing file
    into an ambiguous signal.

    Environment independence — launch the two runs as subprocesses with different `PYTHONHASHSEED`
    values and different working directories, still writing to two distinct `--out` roots, and
    assert the digests still match. Python randomizes string hashing per process and RESEARCH names a
    pinned hash seed as an insufficient safeguard on its own. Note that the working directory is the
    varied *environment*, not the mechanism that separates the two outputs — plan 01-06 anchors
    `DATA_ROOT` to the storage module's own location rather than to the process working directory
    precisely so that this assertion and the two-output-roots assertion above remain two independent
    experiments rather than the same one written twice.

    Interruption — start a run, kill it partway through the ingest stage, and assert `data/canonical/`
    contains no partially-written table and no leftover temporary file, then assert a subsequent
    clean run completes and produces the expected digests. Assert the same for `data/lineage/`: a
    half-written fragment would corrupt the provenance of tables that did complete.

    Stage isolation — after a full run, re-run the `features` stage alone and assert that
    `data/lineage/simulate.parquet` and `data/lineage/ingest.parquet` have unchanged sha256 digests
    while `data/lineage/features.parquet` is rewritten to the same bytes. This is the runtime
    counterpart to Task 1's static `dvc.yaml` overlap check: the static check proves nothing is
    *declared* twice, and this proves nothing is *written* twice.

    Create `tests/integration/test_config_drives_behavior.py` running the `tiny` profile end to end,
    then mutating exactly one YAML value at a time into a temporary config tree and re-running:
    a seasonality multiplier, a latent-trait distribution parameter, and one entry in the
    `config/features.yaml` feature list. Assert for each that the output digest changes and that the
    config hash changes, and assert that no file under `src/` was modified during the test. This is
    phase success criterion 5's second half — a reviewer can change the seasonality curve, the
    feature list, or a simulator parameter by editing schema-validated YAML rather than code.

    Extend `.github/workflows/ci.yml` by appending to the existing `checks` job, after the `pytest`
    step: a step running `just reproduce profile="ci"`, a step running
    `uv run pytest tests/golden -q -m slow`, and a **memory-budget step** running the three budget
    suites on the `demo` profile with `-rs` so skips are reported, then failing the job if any peak-RSS
    assertion was skipped. Keep the existing steps unchanged and keep the Linux-only,
    no-failure-suppression posture from plan 01-04.

    The skip guard is the point of that third step rather than a detail of it. Plans 01-08, 01-09 and
    01-10 each assert peak process RSS, which is the only instrument that can observe the Arrow and
    DuckDB half of ENG-08's budget, and each is specified to skip with a stated reason where the
    platform provides no `getrusage` reading — which is the case on the Windows development machine.
    A gate that is permanently skipped on the only machine anyone runs it on is not a gate, and this
    phase has already shipped three consecutive amendments in which the memory claim outran the thing
    measuring it. D-16 makes Linux the enforcing platform, so CI is where those three assertions must
    be required to actually execute. Implement the guard by parsing pytest's short summary for skipped
    tests whose node id names a peak-RSS assertion and exiting non-zero if any is found; assert the
    step is not `continue-on-error`.
  </action>
  <verify>
    <automated>uv run pytest tests/golden/test_reproducibility.py tests/integration/test_config_drives_behavior.py -q -m slow</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/golden/test_reproducibility.py -q -m slow` exits 0
    - `uv run pytest tests/integration/test_config_drives_behavior.py -q` exits 0
    - The golden suite asserts equal digests for all twelve produced Parquet files — the nine data tables plus the three lineage fragments — and names the diverging file on failure
    - The golden suite asserts the discovered file set equals the expected twelve rather than only iterating whatever it finds
    - **(HIGH-6, two roots are real)** The golden suite obtains its two output trees by passing `--out` to each of the three stage modules, asserts neither tree contains a file belonging to the other, and asserts the repository's own `data/` directory is unmodified by the test (`git status --porcelain data` empty, or the directory absent)
    - **(HIGH-6)** The golden suite's environment-independence assertion varies `PYTHONHASHSEED` and the working directory *while still using two distinct `--out` roots*, so it remains a different experiment from the two-roots assertion
    - **(HIGH-5 consequence)** The golden suite asserts no discovered path lies under `_staging` in either root, and that neither root retains a staging directory after its run
    - The golden suite contains the different-seed assertion proving different seeds produce different bytes
    - The golden suite contains the zero-row table assertion including the schema comparison
    - The golden suite launches its two runs with differing `PYTHONHASHSEED` values
    - The golden suite contains the interrupted-run assertion checking for both partial tables and leftover temporary files, in `data/canonical/` and in `data/lineage/`
    - **(HIGH-3 runtime proof)** The golden suite re-runs the `features` stage alone and asserts the `simulate` and `ingest` lineage fragments are byte-unchanged
    - The config suite mutates a seasonality multiplier, a trait parameter and a feature-list entry, asserting a changed digest and a changed config hash for each
    - The config suite asserts `git status --porcelain src/` is empty after the run
    - `.github/workflows/ci.yml` contains a step invoking `just reproduce` with the `ci` profile and a step running the golden suite
    - **(HIGH-10, the RSS gate cannot vanish by permanent skip)** `.github/workflows/ci.yml` contains a step running the three budget suites on the `demo` profile and failing the job if any peak-RSS assertion was skipped, and a test asserts that step exists and is not `continue-on-error`. Without it, all three plans' RSS assertions could skip forever on the development platform and the phase would again be claiming a memory bound nothing measured
    - The existing lint, import-contract and pytest steps in the workflow are unchanged
    - `grep -c 'continue-on-error' .github/workflows/ci.yml` outputs `0`
  </acceptance_criteria>
  <done>Byte-identical reruns are proven across seeds, hash seeds, empty tables and interruptions; a YAML edit provably changes output; CI enforces all of it.</done>
</task>

<task type="auto">
  <name>Task 3: Write SIMULATOR_ASSUMPTIONS.md and its structural floor check</name>
  <files>docs/SIMULATOR_ASSUMPTIONS.md, tests/unit/test_simulator_assumptions_doc.py</files>
  <read_first>
    - `src/nextmove/simulator/traits.py` — the module docstring recording each latent trait's
      distribution family, parameters, and the behavioural meaning of a high value
    - `src/nextmove/simulator/response.py` — the `response_multiplier` combining formula docstring,
      the `fatigue_penalty` docstring naming the D-04 loophole, and `base_conversion_probability`
    - `src/nextmove/simulator/tick.py` — the organic generation logic, micro-event rates and the
      dwell-class boundaries
    - `src/nextmove/simulator/world.py` — the seasonality application and the high-versus-low
      seasonality-class treatment
    - `config/simulator.yaml` — the concrete parameter values every documented assumption must name
      by config key
    - `.planning/phases/01-reproducible-world/01-01-SUMMARY.md` through `01-10-SUMMARY.md` — each
      plan's SUMMARY records the parameter choices made during execution; those are the authoritative
      values, not the planning documents
    - `.planning/REQUIREMENTS.md` — SIM-03 verbatim (every generative assumption documented, and
      every result derived from simulated data labeled "in simulation" wherever it is reported)
    - `docs/TECHNICAL_DIRECTION.md` section 2 — the four simulator honesty rules
    - `docs/QUALITY_BAR.md` section 5 — the documentation standard that names
      `SIMULATOR_ASSUMPTIONS.md` as a required artifact
  </read_first>
  <action>
    Write `docs/SIMULATOR_ASSUMPTIONS.md` in a fixed section order, which the floor check pins:
    `## World Structure`, `## Latent Traits`, `## Seasonality`, `## Inventory`, `## Campaigns`,
    `## Organic Behaviour`, `## Micro-Conversion Events`, `## Action Response`,
    `## The Fatigue Loophole`, `## Determinism`, `## Known Limitations`.

    Every entry states four things: the assumption in plain prose, the exact parameter values as
    shipped, the `config/simulator.yaml` key the value is read from, and why the assumption was made
    (or, honestly, that it is a design choice with no external validation). RESEARCH assumption A3
    and A4 make this explicit — the latent-trait distribution families were the planner's choice, and
    the general latent-trait pattern is supported by retail-simulation literature while the exact
    functional forms are not externally validated. Say so rather than implying more support than
    exists.

    Content the document must cover: the fashion-core plus one low-seasonality contrast category
    structure and why the contrast category exists; the distribution family, parameters and
    behavioural meaning of each of price sensitivity, loyalty, fatigue propensity and category
    affinity; the twelve monthly seasonality multipliers and how the high and low seasonality classes
    are treated differently; inventory initialization and restock behaviour; campaign generation and
    audience selection; the session-occurrence, browse-length, add-to-cart, order and abandonment
    generation rules; the micro-event emission rates and the dwell-class boundaries, together with
    the explicit statement that Phase 1 emits raw micro-events only and derives no
    micro-action-to-conversion weight, since that derivation is Phase 4 work per D-06; the
    `response_multiplier` combining formula written out; the fatigue-penalty form; the deliberate,
    config-disableable fatigue penalty as D-04's documented reward-hacking loophole with the note
    that the probe exploiting it arrives with the contextual bandit in v2; the seed layout and why
    seeds live in config rather than on the command line; and a Known Limitations section naming what
    the simulator does not model.

    Under `## Known Limitations`, document the ground-truth uplift sampling cadence explicitly: that
    `data/ground_truth/ground_truth_uplift` is a *precomputed convenience table sampled every*
    `simulator.uplift_snapshot_every_ticks` *ticks* rather than a per-tick materialization, that the
    shipped default of 30 yields roughly 18 snapshots and on the order of 7.2 million rows at default
    scale, that the per-tick alternative would be roughly 219 million rows and fails ENG-08's laptop
    guarantee, and — the part that matters for honesty — that `ground_truth_uplift` the *function*
    remains callable for any (customer, action, tick) triple, so the sampling is a storage choice and
    not a narrowing of D-03 or MODEL-06. Name the config key so a reader can change the cadence
    without a code edit. A reader who finds a multi-million-row ground-truth table with no stated
    sampling rate cannot tell a design choice from an accident.

    Add a prominent statement near the top that every result derived from this simulator is labeled
    "in simulation" wherever it is reported, per SIM-03 and TECHNICAL_DIRECTION's simulator honesty
    rules.

    Where two assumptions describe adjacent parameter ranges — for example two dwell classes, or two
    discount tiers — document them as separate entries with explicit non-overlapping ranges and state
    which side each boundary belongs to. A merged or ambiguous range is where a reader silently
    invents the boundary rule.

    Create `tests/unit/test_simulator_assumptions_doc.py` asserting: the file exists; all eleven
    required headings are present in the declared order; each section body contains at least one
    non-blank non-heading line, so an empty section fails; the file has at least 120 non-blank lines;
    it contains the phrase `in simulation`; it names every top-level key present in
    `config/simulator.yaml`, computed by loading the YAML and checking each key appears in the
    document text, so a newly-added config parameter cannot ship undocumented; and it mentions the
    fatigue loophole.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_simulator_assumptions_doc.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_simulator_assumptions_doc.py -x -q` exits 0
    - `docs/SIMULATOR_ASSUMPTIONS.md` contains all eleven required headings in the declared order
    - Every section body contains at least one non-blank, non-heading line
    - The file has at least 120 non-blank lines
    - The file contains the phrase `in simulation`
    - The test loads `config/simulator.yaml` and asserts every top-level key under `simulator:` appears somewhere in the document, so an undocumented parameter fails the build
    - **(MEDIUM-4)** That floor check therefore covers `uplift_snapshot_every_ticks`, which plan 01-08 made a `simulator:` config key precisely so a structural check could reach it; a cadence living as a code constant would have been structurally invisible to this test
    - **(MEDIUM-4)** `## Known Limitations` states the `ground_truth_uplift` sampling cadence, its shipped default, the resulting approximate row count, the per-tick row count it avoids, and that the uplift *function* remains callable for any (customer, action, tick) triple — asserted by a test grepping that section for `uplift_snapshot_every_ticks`
    - The document names the fatigue loophole and states that the probe exploiting it is v2
    - The document states that Phase 1 emits raw micro-events only and derives no micro-conversion weight
    - Each documented parameter names the `config/simulator.yaml` key it is read from
    - `uv run pytest tests -q` reports zero failures across the whole suite
  </acceptance_criteria>
  <done>Every generative assumption is written down, tied to its config key, and a floor check prevents the document from silently going stale.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (plan 01-11)

**New files:** `dvc.yaml`, `src/nextmove/storage/__main__.py`, `docs/SIMULATOR_ASSUMPTIONS.md`,
`tests/golden/test_reproducibility.py`, `tests/integration/test_config_drives_behavior.py`,
`tests/unit/test_simulator_assumptions_doc.py`.

**New DVC stages:** `simulate`, `ingest`, `features` (with a profile variable), named to match the
three `Stage` members from plan 01-06.

**New DVC stage outputs (each declared by exactly one stage, none `persist`):** `simulate` →
`data/raw/`, `data/ground_truth/`, `data/lineage/simulate.parquet`; `ingest` → `data/canonical/`,
`data/lineage/ingest.parquet`; `features` → `data/features/`, `data/lineage/features.parquet`.
No stage declares `data/lineage/` as a directory out, and no lineage fragment is any stage's dep.

**New DVC stage dependencies (review MEDIUM-9):** all three stages now declare
`src/nextmove/storage/` and `src/nextmove/config/` among their `deps`, not only `simulate`. Without
them a change confined to a code path the simulator does not exercise — `dedupe_on`'s tie-break rule
for `ingest`, `query`'s pragmas for `features` — would leave `data/raw/` byte-identical and both
downstream stages reported up to date over stale outputs. Neither path is any stage's `out`, so all
four rules of the non-overlap criterion are unaffected.

**Producer→consumer edges the graph requires (asserted present, not forbidden):**
`simulate.outs data/raw/` = `ingest.deps data/raw/`; `ingest.outs data/canonical/` =
`features.deps data/canonical/`. These are how the DAG is wired; the non-overlap check forbids
outs-vs-outs collisions and self-dependency, not these.

**Stated pipeline semantics:** because no out is `persist`, `dvc repro` removes a stage's outs
before re-executing it, so the `ingest` stage rematerializes `data/canonical/` in full from
`data/raw/` on every pipeline run. Plan 01-09's append-only path serves direct invocation. Both are
byte-equal for identical input, asserted here by a forced re-execution digest comparison.

**New / modified `just` recipes:** `reproduce` (placeholder body replaced with the real `dvc repro`
invocation), `budget` (body widened from the simulator suite alone to all three budget suites; the
recipe is 01-08's and its name is unchanged), `lineage` and `clean` (both new). Recipes owned by
other plans and untouched here: `default`, `setup`, `fmt`, `lint`, `test` (01-01), `ci` and the
`lint` extension (01-04), `simulate` (01-08), `ingest` (01-09), `features` (01-10). **Thirteen**
recipes in total after this task, `default` included — `just --list` displays it, so an inventory
omitting it disagrees with the tool.

**New CLI:** `python -m nextmove.storage <table>` with an optional `--root`, printing a lineage
chain from the given output tree.

**New CI steps (appended to the existing `checks` job):** a miniature end-to-end
`just reproduce profile="ci"` step, a golden-comparison step, and a memory-budget step that runs the
three budget suites on the `demo` profile and fails the job if any peak-RSS assertion was skipped —
the guard that keeps ENG-08's binding assertion from evaporating on a platform without `getrusage`.

**New generated artifact:** `dvc.lock`.

**New document sections (`docs/SIMULATOR_ASSUMPTIONS.md`):** `## World Structure`,
`## Latent Traits`, `## Seasonality`, `## Inventory`, `## Campaigns`, `## Organic Behaviour`,
`## Micro-Conversion Events`, `## Action Response`, `## The Fatigue Loophole`, `## Determinism`,
`## Known Limitations`.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published reproducibility claim to reviewer | "byte-identical across two seeded runs" is the phase's headline claim; a gate that cannot detect divergence converts it into a false statement |
| golden fixture to test verdict | A fixture that can be regenerated on failure turns a regression detector into a rubber stamp |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-28 | Repudiation | Golden determinism test that cannot detect divergence | high | mitigate | The suite asserts both halves: equal seeds produce identical digests AND a changed seed produces at least one differing digest. A test asserting only equality would pass for a pipeline that ignores its seeds entirely. Runs are additionally launched under differing `PYTHONHASHSEED` values and working directories so environment-dependent ordering cannot hide. |
| T-01-29 | Tampering | Golden fixture regenerated to make a failing test pass | high | mitigate | The recorded prohibition in this plan's `must_haves.prohibitions` states that a golden update must be an explicit, separately-committed change with its cause stated. No recipe or CLI flag regenerates goldens in place, so a regeneration is necessarily a visible diff in review rather than a side effect of running the suite. |
| T-01-30 | Denial of Service | Interrupted pipeline leaving the workspace in a half-written state | medium | mitigate | Every table is written to a temporary path and atomically renamed by plan 01-06's storage layer; this plan's golden suite proves it by killing a run mid-ingest and asserting neither a partial table nor a leftover temporary file remains in `data/canonical/` or `data/lineage/`, then that a clean rerun still reproduces the expected digests. |
| T-01-33 | Tampering | Overlapping DVC stage outputs silently destroying another stage's artifacts | high | mitigate | Lineage is one fragment per stage in its own zone (plan 01-06), so every declared out belongs to exactly one stage. Two independent checks enforce it: a static script over `dvc.yaml` asserting outs-vs-outs disjointness across stages, no stage depending on its own out, and no lineage fragment appearing as any stage's dep — while *requiring* the two legitimate producer→consumer edges to be present, because a check that forbade those could never pass on a correct DAG and would have been a gate that only ever went red (review HIGH-7). Second, the golden suite re-runs one stage alone and asserts the other stages' fragments are byte-unchanged. Without this, `ingest` re-materializing `data/canonical/` would erase the lineage rows `simulate` wrote, and `dvc.lock` would churn on every run — making phase success criterion 1 undemonstrable. |
| T-01-38 | Repudiation | A reproducibility claim demonstrated by a test that cannot actually distinguish two runs | high | mitigate | The headline golden comparison runs the `ci` profile into two genuinely separate output roots, obtained by passing `--out` to each stage module, which plan 01-06's `root` plumbing and plans 01-08/01-09/01-10's `out_root` forwarding make possible; the suite additionally asserts neither root holds the other's files and that the repository's own `data/` is untouched, so a stage that ignored its root fails rather than passing by writing both runs to one path. `DATA_ROOT` is anchored to the storage module's location rather than the working directory, keeping the two-roots assertion and the two-working-directories assertion independent experiments. Before this, no storage function accepted a root at all and the two-root test was unwritable, which would have left phase success criterion 1 resting on a test that could not be built (review HIGH-6). |
</threat_model>

<verification>
- `uv run dvc repro --dry` lists exactly the three stages; `make reproduce` reaches `just reproduce`
- No two stages declare the same output path, no stage depends on its own output, and no lineage fragment is any stage's dependency — while both producer→consumer edges are present and required
- Two `ci`-profile runs into two `--out` roots are byte-identical across all twelve files, neither root holds the other's files, and a seed change breaks that equality
- Forcing a full stage re-execution leaves the canonical digests unchanged, so pipeline rematerialization and direct append-only landing agree on bytes
- A zero-row table is written with the full schema and is byte-identical across runs
- An interrupted run leaves no partial or temporary file in the canonical or lineage zones
- Re-running one stage leaves the other stages' lineage fragments byte-unchanged
- A change under `src/nextmove/storage/` marks all three stages stale, not only `simulate`
- CI runs the three memory-budget suites and fails if a peak-RSS assertion was skipped
- A second and a third run with unchanged inputs are both no-ops with a byte-identical `dvc.lock`
- Editing one YAML value changes both the output digest and the config hash, with no source edit
- `SIMULATOR_ASSUMPTIONS.md` covers every top-level simulator config key and passes the floor check
- The full suite is green and CI runs the miniature end-to-end plus the golden comparison
</verification>

<success_criteria>
- ENG-04 satisfied: any pipeline stage rerun with the same seed and config hash produces byte-identical outputs, proven not asserted
- ENG-08 satisfied: the whole thing runs on a laptop behind one `just`/`make` command with no cloud dependency
- ENG-09 satisfied: DVC stages version the data artifacts and re-running is idempotent
- SIM-03 satisfied: every generative assumption is documented and tied to its config key
- Phase success criteria 1 and 5 are mechanically true
</success_criteria>

<output>
Create `.planning/phases/01-reproducible-world/01-11-SUMMARY.md` when done. Record the twelve file
digests from the `ci`-profile golden run — the nine data tables and the three lineage fragments — so
a future regression has a reference point. Record the final `dvc.yaml` stage-to-outs *and* stage-to-deps
mapping verbatim, since together they are what make both the no-overlap property, the required
producer→consumer edges and the storage/config staleness coverage auditable. Record the final
`just --list` output verbatim so the recipe inventory in this plan can be checked against reality
rather than against another plan's prose — it has been wrong in two consecutive cycles. Record the
measured peak RSS reported by all three budget suites in the CI memory-budget step, so ENG-08's
phase-level claim has three numbers behind it rather than one.
Record the canonical digests before and after the forced `dvc repro --force` re-execution, since
that pair is the evidence that pipeline rematerialization and direct append-only landing agree on
bytes. Note the two recorded prohibitions (no back-fitted parameters, no silent golden regeneration)
for the phase verification pass.
</output>
