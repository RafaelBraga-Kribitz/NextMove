---
phase: 01-reproducible-world
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/ARCHITECTURAL_DIRECTION.md
  - docs/DECISION_ENGINE_DESIGN.md
  - docs/DESIGN_PRINCIPLES.md
  - docs/OPEN_DECISIONS.md
  - docs/PRODUCT_CHARTER.md
  - docs/PROJECT_IDENTITY.md
  - docs/PROJECT_VISION.md
  - docs/QUALITY_BAR.md
  - docs/RESEARCH_SYNTHESIS.md
  - docs/TECHNICAL_DIRECTION.md
  - docs/README.md
  - docs/adr/001-modular-monolith.md
  - docs/adr/002-hybrid-batch-and-on-demand-decisioning.md
  - docs/adr/003-simulator-as-import-isolated-package.md
  - docs/adr/004-synthetic-decisions-public-data-validation.md
  - docs/adr/005-attributable-models-only-in-decision-path.md
  - docs/adr/006-propensity-delta-uplift-approximation.md
  - docs/adr/007-dvc-and-mlflow-artifact-versioning.md
  - docs/adr/008-static-3wd-thresholds.md
  - docs/adr/009-three-tier-autonomy.md
  - docs/adr/010-fixed-action-archetype-catalog.md
  - tests/unit/test_adr_structure.py
autonomous: true
requirements: [DOC-01]

must_haves:
  truths:
    - "A reviewer can open docs/adr/001 through docs/adr/010 and find every one of OD-1..OD-10 ratified with genuine Context / Decision / Consequences prose (D-10)"
    - "ADR 007 explicitly ratifies OD-7 as DVC for data/artifacts plus MLflow for runs/models, states reviewer recognizability as the deciding factor, and names the accepted cost (D-09)"
    - "ADR 009 ratifies OD-9 as three-tier autonomy and states that tier membership lives in schema-validated YAML rather than code (D-12)"
    - "ADR 004 ratifies OD-4 as recommendation B and states the public-dataset validation track is v2 with no dataset named (D-13)"
    - "The ten root planning documents live under docs/ with git history preserved via git mv, and the repository root holds no planning markdown (D-17)"
    - "Two ADRs that ratify adjacent or related open decisions remain separate numbered files — exactly one ADR per OD, no two ODs merged into one file (edge: adjacency)"
    - "Each ADR file contains non-empty Context, Decision, and Consequences sections; a file with an empty section fails the ADR structure test (edge: empty)"
    - "ADR filenames are zero-padded so lexicographic directory listing order equals decision order 001..010 (edge: ordering)"
  artifacts:
    - path: "docs/adr/007-dvc-and-mlflow-artifact-versioning.md"
      provides: "the ratification that closes the Open OD-7 marker inside locked decision AD-18"
      contains: "OD-7"
      min_lines: 25
    - path: "docs/adr/009-three-tier-autonomy.md"
      provides: "OD-9 ratification defining Tier 1/2/3 autonomy boundaries"
      contains: "Tier 3"
      min_lines: 25
    - path: "docs/README.md"
      provides: "index mapping every relocated document and every ADR"
    - path: "tests/unit/test_adr_structure.py"
      provides: "structural floor check over all ten ADRs"
      min_lines: 25
  key_links:
    - from: "docs/adr/007-dvc-and-mlflow-artifact-versioning.md"
      to: "docs/ARCHITECTURAL_DIRECTION.md"
      via: "ADR 007 cites AD-18 as the locked decision whose open marker it closes"
      pattern: "AD-18"
    - from: "docs/README.md"
      to: "docs/adr/"
      via: "index links each ADR file by number and title"
      pattern: "adr/0"
  prohibitions: []
---

<objective>
Close DOC-01: ratify OD-1 through OD-10 as ten individually-written ADRs under `docs/adr/`, and
relocate the ten root planning documents into `docs/` with `git mv` so history survives (D-17).

Purpose: STATE.md records "OD-1..OD-10 are formally unratified" as the standing Phase 1 gate, and
locked decision AD-18 still carries an open marker for OD-7. This plan is what makes phase success
criterion 5 true — a reviewer can find every ratified open decision written down, including the
explicit OD-7 resolution.
Output: `docs/` containing the relocated corpus, `docs/adr/001`..`010`, an index, and a structural
test that fails if any ADR is a stub.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-reproducible-world/01-CONTEXT.md
@.planning/intel/decisions.md
@.planning/INGEST-CONFLICTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate the ten planning documents into docs/ with history preserved</name>
  <files>docs/ARCHITECTURAL_DIRECTION.md, docs/DECISION_ENGINE_DESIGN.md, docs/DESIGN_PRINCIPLES.md, docs/OPEN_DECISIONS.md, docs/PRODUCT_CHARTER.md, docs/PROJECT_IDENTITY.md, docs/PROJECT_VISION.md, docs/QUALITY_BAR.md, docs/RESEARCH_SYNTHESIS.md, docs/TECHNICAL_DIRECTION.md, docs/README.md</files>
  <read_first>
    - `.planning/phases/01-reproducible-world/01-CONTEXT.md` — decision D-17 (the move is via
      `git mv`, history preserved, alongside the new `docs/adr/`) and the Canonical References
      section (which documents are load-bearing for later phases and must stay findable)
    - `QUALITY_BAR.md` section 5 "Documentation standards" — the standard D-17 satisfies:
      `docs/` holds the foundation docs, ADRs live at `docs/adr/NNN-*.md`
  </read_first>
  <action>
    Create the `docs/` directory and move all ten root planning markdown files into it using
    `git mv` — never a copy-then-delete, because `git log --follow` must continue to resolve each
    file's history. The ten files are: ARCHITECTURAL_DIRECTION.md, DECISION_ENGINE_DESIGN.md,
    DESIGN_PRINCIPLES.md, OPEN_DECISIONS.md, PRODUCT_CHARTER.md, PROJECT_IDENTITY.md,
    PROJECT_VISION.md, QUALITY_BAR.md, RESEARCH_SYNTHESIS.md, TECHNICAL_DIRECTION.md.

    Do not edit the contents of the ten documents. In particular, leave the open-decision marker
    for OD-7 inside ARCHITECTURAL_DIRECTION.md section 8 exactly as written — ADR 007 supersedes it
    by reference. Editing a locked document in place would destroy the audit trail showing the
    marker ever existed.

    Create `docs/README.md` as an index containing two tables. Table one lists the ten relocated
    foundation documents, each with a one-line purpose and a note that it moved from the repository
    root during Phase 1 per D-17. Table two lists all ten ADR filenames fixed by this plan, each
    with its OD number and a one-line decision summary. Write the ADR table against the filenames
    declared in this plan's `files_modified`, then confirm it after Task 3.

    Do not create a root `README.md` — that is DOC-02, scoped to Phase 5.
  </action>
  <verify>
    <automated>test -d docs && for f in ARCHITECTURAL_DIRECTION DECISION_ENGINE_DESIGN DESIGN_PRINCIPLES OPEN_DECISIONS PRODUCT_CHARTER PROJECT_IDENTITY PROJECT_VISION QUALITY_BAR RESEARCH_SYNTHESIS TECHNICAL_DIRECTION; do test -f "docs/$f.md" || exit 1; test ! -e "$f.md" || exit 1; done; test "$(git log --follow --oneline -- docs/QUALITY_BAR.md | wc -l)" -ge 2</automated>
  </verify>
  <acceptance_criteria>
    - All ten files exist at `docs/NAME.md` and none of them exists at the repository root
    - `git log --follow --oneline -- docs/QUALITY_BAR.md` returns at least 2 commits, proving the rename was recorded rather than a delete-plus-add
    - `git status --porcelain` shows the moves staged as renames (status letter `R`), not as `D` plus `??`
    - `ls *.md` at the repository root lists no planning document
    - `docs/README.md` exists and contains the substring `adr/0`
    - The contents of the ten relocated documents are byte-identical to their pre-move contents (`git diff --cached -M --stat` shows 100% similarity for each rename)
  </acceptance_criteria>
  <done>The repository root holds only tooling files; the foundation corpus lives under `docs/` with intact history.</done>
</task>

<task type="auto">
  <name>Task 2: Write ADRs 001-005 ratifying OD-1 through OD-5</name>
  <files>docs/adr/001-modular-monolith.md, docs/adr/002-hybrid-batch-and-on-demand-decisioning.md, docs/adr/003-simulator-as-import-isolated-package.md, docs/adr/004-synthetic-decisions-public-data-validation.md, docs/adr/005-attributable-models-only-in-decision-path.md</files>
  <read_first>
    - `docs/OPEN_DECISIONS.md` — the authoritative text of OD-1 through OD-5 with their options,
      trade-offs and recommendations. Every ADR must reproduce the real trade-off reasoning, not
      restate the recommendation line.
    - `.planning/phases/01-reproducible-world/01-CONTEXT.md` — decisions D-10 (individually written
      ADRs with genuine Context/Decision/Consequences, no batch rubber-stamping), D-11 (OD-1, 2, 3,
      5 are ratified as recommended without re-litigation, each already substantively encoded in a
      locked ADR), D-13 (OD-4 ratified as recommendation B, validation track is v2)
    - `.planning/intel/decisions.md` — the locked AD-* decisions each OD is already encoded in, so
      each ADR can cite its precedent: OD-1 to AD-14, OD-2 to AD-11 plus TD-03/TD-04, OD-3 to AD-12,
      OD-5 to AD-09/AD-15
  </read_first>
  <action>
    Create `docs/adr/` and write five ADRs. Every file uses the same five-heading structure, in this
    order: `# ADR NNN: <title>`, then `## Status`, `## Context`, `## Decision`, `## Consequences`,
    `## Alternatives Considered`. `## Status` states `Accepted — 2026-07-25 (ratifies OD-N)` plus
    the locked AD-* decision it derives from, when one exists.

    - `001-modular-monolith.md` — ratifies OD-1 as option A: a modular monolith in one container,
      with registry interfaces designed service-ready and boundary discipline enforced by
      import-linting between packages. Consequences must name the risk the recommendation
      acknowledges (blurred boundaries without discipline) and the concrete mitigation shipping in
      this same phase (the SIM-02 import-linter contract, plan 01-04). Cite locked AD-14.
    - `002-hybrid-batch-and-on-demand-decisioning.md` — ratifies OD-2 as option C: batch is the
      operational truth (the CRM feed export), and the same engine invoked synchronously powers the
      interactive path. One engine, two invocation modes. Consequences must state that this is the
      decision D-19's dual feature path implements in Phase 1 — a daily materialized grid plus the
      same transform functions exposed for on-demand computation at an arbitrary `as_of_ts`. Cite
      locked AD-11 and proposed TD-03/TD-04.
    - `003-simulator-as-import-isolated-package.md` — ratifies OD-3 as option A: same repository,
      isolated package, lint-enforced import ban. Context must state the contamination risk in the
      source's own terms: if simulator internals are importable by models, results are fake.
      Decision must name the exact contract shipping in plan 01-04 — the four forbidden source
      packages `nextmove.models`, `nextmove.decisions`, `nextmove.policies`, `nextmove.features`
      and the forbidden module `nextmove.simulator`. Cite locked AD-12.
    - `004-synthetic-decisions-public-data-validation.md` — ratifies OD-4 as option B per D-13:
      decisions are evaluated in simulation and always labeled "in simulation"; data engineering is
      additionally validated against a public e-commerce clickstream dataset. Decision must state
      explicitly that the validation track is v2 and that no dataset is named yet. Consequences
      must state Phase 1's only obligation under this ADR — DATA-01's rule that ingestion adapters
      map external shapes inward to the canonical schema and never the reverse — and note that the
      `IngestAdapter` registry shipping in plan 01-05 is the seam that keeps the v2 track cheap.
    - `005-attributable-models-only-in-decision-path.md` — ratifies OD-5 as option A: the decision
      path requires attributions; black-box challengers may appear in the comparison study only.
      Consequences must include the source's own framing that "we declined X% accuracy for faithful
      explanations" is itself a strong result. Cite locked AD-09/AD-15.

    Each ADR needs at least 25 non-blank lines of real prose. A file that only restates the
    recommendation sentence violates D-10 and fails the min-length floor in Task 3's test.
  </action>
  <verify>
    <automated>for n in 001 002 003 004 005; do f=$(ls docs/adr/$n-*.md) || exit 1; for h in '## Status' '## Context' '## Decision' '## Consequences'; do grep -qF "$h" "$f" || exit 1; done; test "$(grep -vc '^[[:space:]]*$' "$f")" -ge 25 || exit 1; done; grep -qF 'nextmove.simulator' docs/adr/003-simulator-as-import-isolated-package.md; grep -qiF 'v2' docs/adr/004-synthetic-decisions-public-data-validation.md</automated>
  </verify>
  <acceptance_criteria>
    - Five files exist matching `docs/adr/00[1-5]-*.md`
    - Each contains all four headings `## Status`, `## Context`, `## Decision`, `## Consequences`
    - Each has at least 25 non-blank lines
    - `docs/adr/003-simulator-as-import-isolated-package.md` names all four forbidden source packages and `nextmove.simulator`
    - `docs/adr/004-synthetic-decisions-public-data-validation.md` states the validation track is v2 and names no concrete dataset
    - `docs/adr/002-hybrid-batch-and-on-demand-decisioning.md` contains the string `as_of_ts`
    - Each `## Status` line names the OD number it ratifies
  </acceptance_criteria>
  <done>OD-1 through OD-5 are ratified in writing with real trade-off reasoning and cited precedents.</done>
</task>

<task type="auto">
  <name>Task 3: Write ADRs 006-010 and the ADR structure test</name>
  <files>docs/adr/006-propensity-delta-uplift-approximation.md, docs/adr/007-dvc-and-mlflow-artifact-versioning.md, docs/adr/008-static-3wd-thresholds.md, docs/adr/009-three-tier-autonomy.md, docs/adr/010-fixed-action-archetype-catalog.md, tests/unit/test_adr_structure.py, docs/README.md</files>
  <read_first>
    - `docs/OPEN_DECISIONS.md` — authoritative text of OD-6 through OD-10
    - `.planning/phases/01-reproducible-world/01-CONTEXT.md` — decisions D-09 (OD-7 ratified as DVC
      plus MLflow, deciding factor reviewer recognizability, accepted cost workflow friction plus a
      second tool), D-11 (OD-6, 8, 10 ratified as recommended, encoded in AD-08, AD-10, AD-06),
      D-12 (OD-9 ratified as three-tier autonomy with tier membership in schema-validated YAML)
    - `docs/adr/001-modular-monolith.md` — written in Task 2; match its heading structure exactly so
      the structure test is uniform
    - `.planning/REQUIREMENTS.md` — DEC-03's action catalog wording (about eight archetypes) so
      ADR 010 matches the requirement it governs
  </read_first>
  <action>
    Write five more ADRs using the identical five-heading structure from Task 2.

    - `006-propensity-delta-uplift-approximation.md` — ratifies OD-6 as option A: MVP approximates
      uplift as P(convert|action) minus P(convert|none) from response models, with the
      approximation error against simulator ground truth quantified and promoted to a headline
      evaluation section. Consequences must state the Phase 1 obligation this creates: D-03's
      latent-trait response functions must make ground-truth uplift computable per
      (customer, action) pair, which is what makes the error measurable at all. Cite locked AD-08.
    - `007-dvc-and-mlflow-artifact-versioning.md` — ratifies OD-7 as DVC for data and artifacts plus
      MLflow for runs and models, per D-09. `## Status` must state that this ADR closes the open
      marker inside locked decision AD-18 in ARCHITECTURAL_DIRECTION.md section 8, and the file must
      contain the literal string `AD-18`. `## Decision` must state reviewer recognizability as the
      deciding factor — a hiring manager knows DVC on sight whereas a custom hash-store needs
      explaining — and must name the rejected alternative (a content-hashed artifact store plus
      MLflow artifacts). `## Consequences` must name the accepted costs verbatim in substance: some
      workflow friction and a second tool to install. It must also state the Phase 1 obligation:
      canonical event and feature Parquet tables are DVC-tracked per D-18, and DATA-04's per-table
      content hashes stay meaningful because DVC versions immutable content-addressable files
      rather than a mutable multi-gigabyte database blob.
    - `008-static-3wd-thresholds.md` — ratifies OD-8 as static in MVP with the threshold interface
      designed for dynamism, and dynamic thresholds as the first post-MVP experiment. Cite locked
      AD-10.
    - `009-three-tier-autonomy.md` — ratifies OD-9 as three-tier autonomy per D-12. Decision must
      define all three tiers concretely: Tier 1 auto-applies on a 3WD Accept verdict and covers
      content and ranking micro-changes (variant assignments, ranking-weight nudges); Tier 2
      requires human sign-off and covers policy and model promotions and segment redefinitions;
      Tier 3 is human-only and never automatic, covering anything touching price or discount
      ceilings. Decision must state that tier membership lives in schema-validated YAML per ENG-03
      — specifically `config/autonomy.yaml` created in plan 01-03 — so the autonomy boundary is
      reviewable config, not code. The file must contain the literal string `Tier 3`.
    - `010-fixed-action-archetype-catalog.md` — ratifies OD-10: the MVP fixes about eight action
      archetypes (none, wait, recommend, bundle, two discount tiers, email-now, email-delayed) with
      small parameter grids, and the `ActionProvider` registry is the escape hatch. Cite locked
      AD-06 and note the resolved timing/channel split recorded in `.planning/INGEST-CONFLICTS.md`
      — timing and channel are fixed archetype parameters in v1 per DEC-04.

    Then write `tests/unit/test_adr_structure.py` covering all ten ADRs: parametrize over the
    numbers `001` through `010`, resolve each to exactly one file under `docs/adr/` by glob
    (asserting exactly one match per number, which is what enforces one-ADR-per-OD and no merged
    files), and assert for each that the four required headings are present, that the section body
    between each heading and the next contains at least one non-blank, non-heading line, and that
    the file has at least 25 non-blank lines. Add one non-parametrized test asserting that sorting
    the ADR filenames lexicographically yields the same sequence as sorting them by their integer
    number, which is what the zero-padding guarantees.

    Finally, update `docs/README.md` so its ADR table matches the ten files actually written.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_adr_structure.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_adr_structure.py -x -q` exits 0
    - `ls docs/adr/*.md | wc -l` outputs `10`
    - `grep -lF 'AD-18' docs/adr/007-dvc-and-mlflow-artifact-versioning.md` returns the file
    - `grep -cF 'Tier 3' docs/adr/009-three-tier-autonomy.md` is at least 1
    - `grep -F 'config/autonomy.yaml' docs/adr/009-three-tier-autonomy.md` matches
    - `grep -iF 'recogniz' docs/adr/007-dvc-and-mlflow-artifact-versioning.md` matches (the stated deciding factor)
    - `tests/unit/test_adr_structure.py` asserts exactly one file matches each of the numbers 001..010, so merging two ODs into one ADR fails the test
    - `tests/unit/test_adr_structure.py` asserts each section body is non-empty, so an ADR with an empty `## Consequences` fails
    - `docs/README.md` ADR table lists all ten filenames
  </acceptance_criteria>
  <done>All ten ODs are ratified; the structure test proves none of them is a stub and none was merged with another.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (plan 01-02)

**New directories:** `docs/`, `docs/adr/`.

**Relocated files (git mv from repository root):** `docs/ARCHITECTURAL_DIRECTION.md`,
`docs/DECISION_ENGINE_DESIGN.md`, `docs/DESIGN_PRINCIPLES.md`, `docs/OPEN_DECISIONS.md`,
`docs/PRODUCT_CHARTER.md`, `docs/PROJECT_IDENTITY.md`, `docs/PROJECT_VISION.md`,
`docs/QUALITY_BAR.md`, `docs/RESEARCH_SYNTHESIS.md`, `docs/TECHNICAL_DIRECTION.md`.

**New files:** `docs/README.md`, `docs/adr/001-modular-monolith.md`,
`docs/adr/002-hybrid-batch-and-on-demand-decisioning.md`,
`docs/adr/003-simulator-as-import-isolated-package.md`,
`docs/adr/004-synthetic-decisions-public-data-validation.md`,
`docs/adr/005-attributable-models-only-in-decision-path.md`,
`docs/adr/006-propensity-delta-uplift-approximation.md`,
`docs/adr/007-dvc-and-mlflow-artifact-versioning.md`, `docs/adr/008-static-3wd-thresholds.md`,
`docs/adr/009-three-tier-autonomy.md`, `docs/adr/010-fixed-action-archetype-catalog.md`,
`tests/unit/test_adr_structure.py`.

**New document convention:** the ADR heading contract `## Status` / `## Context` / `## Decision` /
`## Consequences` / `## Alternatives Considered`, enforced by `tests/unit/test_adr_structure.py`.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git history to reviewer | The provenance claim ("these decisions were ratified, not retrofitted") rests entirely on commit history surviving the move |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Repudiation | `git mv` of the ten foundation documents | medium | mitigate | Use `git mv` exclusively so renames are recorded with 100% similarity; Task 1's acceptance criteria assert `git log --follow` still resolves at least two commits for a moved file and that `git status` reports `R` rather than `D` plus untracked. A copy-then-delete would silently sever the audit trail that these documents predate the ADRs. |
| T-01-04 | Tampering | Editing a locked foundation document in place while relocating it | medium | mitigate | The action forbids content edits during the move, including removing the OD-7 open marker in ARCHITECTURAL_DIRECTION.md; the rename-similarity acceptance criterion mechanically detects any content change. ADR 007 supersedes by reference instead. |
</threat_model>

<verification>
- Repository root contains no planning markdown; all ten live under `docs/`
- `git log --follow` resolves history for every relocated document
- Ten ADR files exist, one per OD, each with four non-empty required sections and at least 25 non-blank lines
- ADR 007 names AD-18, DVC, MLflow, and the recognizability rationale
- ADR 009 defines all three autonomy tiers and points at `config/autonomy.yaml`
- `uv run pytest tests/unit/test_adr_structure.py` is green
</verification>

<success_criteria>
- DOC-01 satisfied: OD-1..OD-10 ratified as `docs/adr/001-*.md` .. `010-*.md`
- OD-7 explicitly resolved in writing as DVC plus MLflow, closing the open marker in AD-18
- D-17 satisfied: the ten planning documents relocated to `docs/` with history preserved
- A structural test prevents any ADR from degrading into a stub
</success_criteria>

<output>
Create `.planning/phases/01-reproducible-world/01-02-SUMMARY.md` when done. Record the final ADR
filenames and, for each OD, one line stating what was ratified.
</output>
