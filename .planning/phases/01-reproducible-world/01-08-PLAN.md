---
phase: 01-reproducible-world
plan: 08
type: execute
wave: 4
depends_on: ["01-05", "01-06", "01-07"]
files_modified:
  - src/nextmove/simulator/response.py
  - src/nextmove/simulator/queue.py
  - src/nextmove/simulator/tick.py
  - src/nextmove/simulator/run.py
  - src/nextmove/simulator/__main__.py
  - src/nextmove/simulator/__init__.py
  - src/nextmove/config/models.py
  - config/simulator.yaml
  - justfile
  - tests/unit/test_response_functions.py
  - tests/unit/test_tick_loop.py
  - tests/integration/test_uc_scenarios_occur.py
  - tests/integration/test_simulation_budget.py
autonomous: true
requirements: [SIM-01, SIM-04]

must_haves:
  truths:
    - "The simulated world responds to delivered actions through explicit action-response functions that are documented functions of a customer's latent traits plus context, so ground-truth uplift is computable per (customer, action) pair (SIM-01, D-03)"
    - "The world advances in daily ticks that (1) drain a queue of scheduled action deliveries, (2) apply their response functions, then (3) generate that day's organic behaviour — one code path, run in Phase 1 with an empty queue (D-07)"
    - "Because actions are applied before the same tick's organic generation, a delivered action changes downstream organic behaviour in the same day (D-08)"
    - "The simulator emits micro-conversion events — scroll, filter and dwell class — alongside sessions, product views, add-to-cart, orders, campaign exposures, action deliveries and overrides (SIM-04)"
    - "A configurable fatigue penalty can be disabled in config, giving the mandated reward-hacking probe a real loophole to catch, and the loophole is documented rather than hidden (D-04)"
    - "Monetary amounts are integer minor units (cents) throughout; the single site that can produce a fractional cent — discount application — rounds half-to-even and is unit-tested at a .5 boundary (edge: precision)"
    - "The default-scale population genuinely contains UC1 and UC2 customers: a winter-jacket cart abandoner with low stock and low price sensitivity pre-Christmas, and a recently-purchased satisfied high-fatigue customer — found by query, not hand-built (D-05)"
    - "run_simulation's peak Python heap is bounded by the flush interval rather than by the horizon: events are flushed to staging part-files through write_part_file and never all held at once, so the 50k-customer 548-day default run fits a laptop (ENG-08, review MEDIUM-1, review HIGH-5)"
    - "The flush interval changes nothing about the output: two runs of the same profile with different flush intervals produce byte-identical tables, so the memory control cannot become a determinism hazard (ENG-04, review MEDIUM-1)"
    - "A stated peak-memory and wall-clock budget for each profile is asserted by a test rather than assumed, and the default profile's budget is runnable on demand rather than only in theory (ENG-08)"
    - "run_simulation's out_root reaches disk: every table and every part file it writes resolves under the root it was given, so two runs of the same profile into two roots can be compared byte for byte without one overwriting the other (ENG-04, review HIGH-6)"
    - "Every table this plan writes declares a sort key whose tuple is unique across its rows — including the two ground-truth tables, which previously declared none — so no write reaches the storage layer missing a required argument (ENG-04, review HIGH-8)"
    - "The cadence at which ground_truth_uplift is sampled is a schema-validated YAML value inside the hashed config surface, not a code literal, because changing it legitimately changes the shipped table's bytes and the lineage row must be able to account for that (ENG-03, review MEDIUM-3)"
  artifacts:
    - path: "src/nextmove/simulator/response.py"
      provides: "documented action-response functions and computable ground-truth uplift"
      exports: ["ActionType", "response_multiplier", "ground_truth_uplift", "fatigue_penalty"]
      min_lines: 110
    - path: "src/nextmove/simulator/queue.py"
      provides: "the scheduled action-delivery queue Phase 3 replay fills"
      exports: ["ScheduledAction", "ActionQueue"]
      min_lines: 40
    - path: "src/nextmove/simulator/tick.py"
      provides: "the three-stage daily tick and organic event generation including micro-events"
      exports: ["advance_tick", "TickResult"]
      min_lines: 180
    - path: "src/nextmove/simulator/run.py"
      provides: "full-horizon run with periodic flushing to staging part files, writing raw event and ground-truth tables through the storage layer under an addressable output root"
      exports: ["run_simulation", "FLUSH_EVERY_TICKS"]
      min_lines: 90
    - path: "tests/integration/test_uc_scenarios_occur.py"
      provides: "D-05 acceptance target — UC1 and UC2 profiles genuinely occur"
      min_lines: 50
    - path: "tests/integration/test_simulation_budget.py"
      provides: "peak-heap and wall-clock budget enforcement, flush-interval invariance, and the on-demand default-profile budget check"
      min_lines: 60
  key_links:
    - from: "src/nextmove/simulator/tick.py"
      to: "src/nextmove/simulator/queue.py"
      via: "stage 1 of every tick drains the scheduled action queue before any organic generation"
      pattern: "ActionQueue"
    - from: "src/nextmove/simulator/tick.py"
      to: "src/nextmove/ingest/contracts.py"
      via: "every emitted record is constructed as a canonical Event with a typed payload"
      pattern: "Event"
    - from: "src/nextmove/simulator/run.py"
      to: "src/nextmove/storage/repository.py"
      via: "run_simulation persists raw tables through write_table rather than touching Parquet"
      pattern: "write_table"
    - from: "src/nextmove/simulator/run.py"
      to: "src/nextmove/storage/repository.py"
      via: "periodically-flushed part files are merged into the final sorted table by write_table_from_parts, so the flush path shares the single deterministic write site"
      pattern: "write_table_from_parts"
    - from: "src/nextmove/simulator/run.py"
      to: "src/nextmove/storage/repository.py"
      via: "each flush lands through write_part_file, the staging primitive plan 01-06 declares — the destination write_table cannot address because staging is not a Zone"
      pattern: "write_part_file"
    - from: "src/nextmove/simulator/run.py"
      to: "src/nextmove/config/models.py"
      via: "the ground-truth uplift snapshot cadence is read from SimulatorConfig rather than from a module constant, so it sits inside the hashed config surface"
      pattern: "uplift_snapshot_every_ticks"
  prohibitions:
    - statement: "MUST NOT emit, store, or document any micro-conversion weight, coefficient, or micro-action-to-macro-conversion probability as if it were empirically derived — D-06 defers that derivation to Phase 4, so Phase 1 may only emit the raw micro-events themselves"
      status: resolved
      verification: judgment

flagged_assumptions:
  - source: "spec-less edge probe"
    requirement: "SIM-04"
    category: "unclassified"
    status: "unresolved"
    note: "The edge probe returned SIM-04 as unclassified — no edge category could be assigned, so no predicate was auto-derived and none was auto-backstopped. Planner assumption carried forward for human review - the residual edge risk for SIM-04 is judged to be fidelity sufficiency: Phase 1 must emit micro-events at enough resolution that Phase 4 can derive weights empirically, and there is no source artifact stating what resolution is enough. This plan emits dwell as a three-level class plus scroll depth percentage and facet-level filter events, which is the researcher's and planner's judgment of sufficient. If a reviewer believes a different fidelity (for example continuous dwell milliseconds, or per-facet dwell) is required, it is unplanned and must be raised before phase verification."
  - source: "cross-AI review cycle 1 (MEDIUM-1 follow-through)"
    requirement: "SIM-01"
    category: "scale"
    status: "unresolved"
    note: "No source artifact states the materialization cadence of the ground_truth_uplift table. D-03 and MODEL-06 require ground-truth uplift to be *computable* per (customer, action) pair, and the ground_truth_uplift function satisfies that in full for any pair at any tick without running the simulation — that contract is unchanged. What is unspecified is how densely the precomputed convenience table samples it. Materializing every (customer, action, tick) triple at default scale is 50,000 customers x 8 archetypes x 548 days = roughly 219 million rows, which contradicts ENG-08's laptop guarantee on its own, independent of the event-accumulation defect the review found. Planner decision carried forward for human review: materialize one row per (customer, action_type) per snapshot tick on a monthly cadence (the config key simulator.uplift_snapshot_every_ticks, default 30, giving 18 snapshots and roughly 7.2 million rows at default scale), which preserves the seasonal and fatigue variation Phase 4 needs to measure the propensity-delta approximation error. Review cycle 2 (MEDIUM-3) established that this cadence must live in schema-validated YAML rather than as a module constant, because changing it legitimately changes the shipped table's bytes and ENG-04's lineage row can only account for a change that is inside the hashed config surface; it is therefore now a SimulatorConfig field with its default in config/simulator.yaml, and plan 01-11's documentation floor check reaches it automatically because it is a top-level simulator key. If Phase 4 requires per-tick uplift ground truth, that is unplanned at this cadence and must be raised before phase verification — the function is already general, and the change is now a one-line YAML edit plus a storage budget, not a redesign or a code edit."
---

<objective>
Make the world move: action-response functions driven by latent traits (D-03), the deliberate
reward-hacking loophole (D-04), the three-stage daily tick with its action queue (D-07/D-08), organic
behaviour generation including SIM-04's micro-conversion events, and a full-horizon run that writes
the raw tables through the storage layer.

Purpose: this is the plan that makes SIM-01 and SIM-04 true and that produces the data every
subsequent plan consumes. D-07's "one code path, no Phase 3 rewrite" is the deciding design
criterion — the empty-queue Phase 1 run and the policy-driven Phase 3 replay must be the same code,
differing only in queue contents.
Output: `data/raw/` populated with canonical-shaped events plus the ground-truth tables models must
never see.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-reproducible-world/01-CONTEXT.md
@.planning/phases/01-reproducible-world/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement action-response functions, ground-truth uplift, and the fatigue loophole</name>
  <files>src/nextmove/simulator/response.py, src/nextmove/simulator/queue.py, tests/unit/test_response_functions.py</files>
  <read_first>
    - `src/nextmove/simulator/traits.py` and `src/nextmove/simulator/world.py` — written in plan
      01-07; `LatentTraits` field names and the behavioural meaning of each trait as recorded in the
      `traits.py` module docstring
    - `src/nextmove/config/models.py` — `ResponseConfig` and `LoopholeConfig` from plan 01-03
    - `config/simulator.yaml` — the response coefficients and `loophole.fatigue_penalty_enabled`
    - `.planning/phases/01-reproducible-world/01-CONTEXT.md` — D-03 (an action's effect is a
      documented function of latent traits plus context such as inventory and season, and ground-truth
      uplift is therefore computable per customer-action pair, which is what lets Phase 4 measure how
      wrong the propensity-as-uplift approximation is), D-04 (the loophole is Phase 1 scope; the probe
      that exploits it is v2), D-08 (an applied action can change downstream organic behaviour)
    - `.planning/REQUIREMENTS.md` — DEC-03's eight action archetypes (`none`, `wait`, `recommend`,
      `bundle`, two discount tiers, `email-now`, `email-delayed`) and MODEL-06's uplift definition
      `P(convert|action) − P(convert|none)`, which `ground_truth_uplift` must match in shape
    - `docs/adr/006-propensity-delta-uplift-approximation.md` — written in plan 01-02; its
      Consequences section states the obligation this task discharges
  </read_first>
  <behavior>
    - `response_multiplier` for the `none` action returns exactly 1.0 for every customer and context
    - A high-price-sensitivity customer receives a strictly larger multiplier from a discount action than a low-price-sensitivity customer, all else equal
    - A high-loyalty customer receives a larger multiplier from a `recommend` action than a low-loyalty customer, all else equal
    - Accumulated fatigue strictly reduces the multiplier when the fatigue penalty is enabled
    - With `loophole.fatigue_penalty_enabled` set false, the multiplier is independent of accumulated fatigue and the function returns the un-penalized value
    - `ground_truth_uplift(customer, action, context)` equals `P(convert|action) − P(convert|none)` computed from the same functions, and is exactly 0.0 for the `none` action
    - `ground_truth_uplift` is deterministic for a fixed customer, action and context
    - Applying a discount of an odd number of basis points that yields exactly half a cent rounds half-to-even
    - `ActionQueue` drains only actions scheduled at or before the current tick and leaves later ones queued
    - `ActionQueue.drain(tick)` returns deliveries in a deterministic order when several share a tick
  </behavior>
  <action>
    Create `src/nextmove/simulator/queue.py` declaring `ScheduledAction` (frozen: `customer_id`,
    `deliver_tick`, `action_type`, `params`, `origin`) and `ActionQueue` with `schedule`,
    `drain(tick) -> list[ScheduledAction]` and `pending_count`. `drain` returns every action whose
    `deliver_tick` is at or before the given tick, sorted by `(deliver_tick, customer_id,
    action_type)` so ties resolve identically on every run, and removes them from the queue. In
    Phase 1 the queue is constructed empty; Phase 3's policy replay is its first real producer, and
    nothing about this class may assume it stays empty.

    Create `src/nextmove/simulator/response.py`.

    Declare `ActionType` as a StrEnum covering DEC-03's archetypes: `none`, `wait`, `recommend`,
    `bundle`, `discount_low`, `discount_high`, `email_now`, `email_delayed`.

    Declare `apply_discount_cents(price_cents: int, discount_bps: int) -> int` as the project's
    single monetary rounding site. Compute with `decimal.Decimal` and quantize to whole cents with
    `ROUND_HALF_EVEN`. Every other monetary path must call this rather than dividing floats: money is
    carried as integer minor units everywhere else, and a second rounding site would be a second
    place for two runs to disagree in the last digit.

    Declare `fatigue_penalty(traits, fatigue_counter, config) -> float` returning a multiplicative
    factor in the interval 0 through 1 that decreases as the recent-contact counter rises, scaled by
    the customer's `fatigue_propensity`. When `config.loophole.fatigue_penalty_enabled` is false the
    function returns exactly 1.0. Give the function a docstring stating plainly that this toggle is
    D-04's deliberate, documented reward-hacking loophole: a policy that maximizes short-horizon
    profit with the penalty disabled will contact fatigued customers relentlessly, and the probe that
    exploits it arrives with the contextual bandit in v2. Documenting it here is what makes it a
    deliberate artifact rather than a bug someone later finds.

    Declare `response_multiplier(traits, action_type, params, context, config) -> float` — the
    documented function D-03 requires. It combines: a per-archetype base coefficient from
    `ResponseConfig`; a price-sensitivity term that scales the discount archetypes' effect by the
    customer's `price_sensitivity` and the discount magnitude; a loyalty term scaling `recommend`
    and `bundle`; a category-affinity term for the targeted category; a scarcity term reading
    inventory from the context; a seasonality term reading the world's seasonal multiplier; and the
    fatigue penalty. Return exactly 1.0 for `ActionType.none`. Write the combining formula out in the
    docstring in words and symbols — plan 01-11 lifts it into `docs/SIMULATOR_ASSUMPTIONS.md` per
    SIM-03, and an undocumented response function is exactly what TECHNICAL_DIRECTION's simulator
    honesty rules forbid.

    Declare `base_conversion_probability(traits, context, config) -> float` and
    `ground_truth_uplift(traits, action_type, params, context, config) -> float` returning
    `p_convert_with_action − p_convert_with_none`, where the first is the base probability scaled by
    `response_multiplier` and clamped to the interval 0 through 1. Return exactly 0.0 for
    `ActionType.none`. This function is what makes MODEL-06's approximation error measurable in
    Phase 4 — it must be callable for any (customer, action) pair without running the simulation.

    Create `tests/unit/test_response_functions.py` covering every item in the behavior block, with
    the monotonicity claims written as direct comparisons between two constructed customers who
    differ in exactly one trait, and the half-to-even case written as an explicit assertion on a
    price and discount pair that lands exactly on half a cent.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_response_functions.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_response_functions.py -x -q` exits 0
    - `grep -c 'ROUND_HALF_EVEN' src/nextmove/simulator/response.py` is at least 1
    - `apply_discount_cents` is the only function in `src/nextmove/simulator/` that converts a monetary value through a non-integer intermediate, verified by `grep -rn 'cents.*[/]' src/nextmove/simulator` returning only lines inside `apply_discount_cents`
    - `ground_truth_uplift(traits, ActionType.none, ...)` returns exactly `0.0`
    - `response_multiplier(traits, ActionType.none, ...)` returns exactly `1.0`
    - Setting `loophole.fatigue_penalty_enabled` to false makes `response_multiplier` invariant to the fatigue counter, asserted by a test comparing counters 0 and 50
    - `fatigue_penalty` has a docstring containing the word `loophole`
    - `response_multiplier` has a docstring stating the combining formula in words
    - `ActionQueue.drain` returns a deterministically ordered list when several deliveries share a tick
    - `uv run lint-imports` exits 0
  </acceptance_criteria>
  <done>Action response is an explicit, documented function of latent traits and context; ground-truth uplift is computable per pair; the loophole exists and is documented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the three-stage daily tick and organic event generation</name>
  <files>src/nextmove/simulator/tick.py, tests/unit/test_tick_loop.py</files>
  <read_first>
    - `src/nextmove/simulator/response.py` and `src/nextmove/simulator/queue.py` — written in Task 1
    - `src/nextmove/simulator/world.py`, `clock.py`, `rng.py` — written in plan 01-07
    - `src/nextmove/ingest/contracts.py` — written in plan 01-05: the thirteen `EventType` members,
      each payload model's exact fields, `derive_event_id`'s signature including the `seq` argument,
      and `sort_events`
    - `.planning/phases/01-reproducible-world/01-CONTEXT.md` — D-07 (the three ordered stages and
      "one code path, no Phase 3 rewrite" as the deciding criterion), D-08 (actions applied before
      organic generation so a delivery changes the same day's behaviour), D-06 (emit micro-events at
      enough fidelity that weights can later be derived; do not ship guessed weights as derived)
    - `.planning/REQUIREMENTS.md` — SIM-01's event list and SIM-04's micro-event list
    - `.planning/phases/01-reproducible-world/01-RESEARCH.md` — the system architecture diagram's
      tick box, and Pitfall 2 (the simulator should never generate a malformed event by construction)
  </read_first>
  <behavior>
    - `advance_tick` executes its three stages in order: queue drain, response application, organic generation
    - An action delivered on tick N changes the events generated later in tick N for that customer, not only from tick N+1
    - Running a tick with an empty queue produces only organic events and no `action_delivered` events
    - Running a tick with a queued delivery produces exactly one `action_delivered` event per delivery
    - Every emitted record validates as a canonical `Event` with no validation error
    - Every emitted event's `ts` falls strictly inside the tick's own day
    - Events within a session have strictly increasing timestamps
    - A session that produces an add-to-cart without an order produces a `cart_abandon` event
    - An order never contains a sku whose inventory was zero at reservation time
    - Every session emits at least one micro-conversion event, and dwell events carry a class rather than a duration
    - Two runs of the same tick with the same world and seeds produce identical event lists including identical `event_id` values
  </behavior>
  <action>
    Create `src/nextmove/simulator/tick.py`.

    Declare `TickResult` (frozen: `tick`, `events: list[Event]`, `fatigue_deltas: dict[int, int]`,
    `inventory_movements`). Declare
    `advance_tick(world, tick, queue, config) -> TickResult` implementing exactly the three D-07
    stages in this order, with the order enforced by structure and asserted by test:

    Stage 1 — drain `queue.drain(tick)`. For each delivery emit an `action_delivered` event and
    record the delivery against the customer's fatigue counter.
    Stage 2 — apply each delivery's `response_multiplier` to that customer's propensity state for
    this tick. This must happen before stage 3, because D-08's closed-loop story depends on a
    delivery being able to change the same day's organic behaviour, not merely the next day's.
    Stage 3 — generate organic behaviour for every active customer.

    Organic generation per customer, drawing from `stream_rng(SeedDomain.organic, customer_id, tick,
    seeds)` so the customer's stream is independent of every other customer and of the visit order:
    decide session occurrence from the customer's base visit propensity scaled by the seasonal
    multiplier, the customer's affinity for the categories on offer, and any stage-2 response boost;
    emit `session_start`; generate a browse sequence of `product_view` events over SKUs weighted by
    category affinity; interleave the SIM-04 micro-conversion events — `scroll` carrying an integer
    depth percentage, `filter_apply` carrying a facet and value, and `dwell` carrying one of the
    three configured dwell classes — at configured rates; possibly emit `add_to_cart`; then either
    `order_placed` (reserving inventory, and if the reservation cannot be filled, not placing the
    order) or, once the session ends with a non-empty cart, `cart_abandon`; emit `session_end`. Emit
    `campaign_exposure` for customers in an active campaign's audience.

    Attach no weight, coefficient, score or conversion probability to any micro-conversion event.
    D-06 defers deriving how much a micro-action predicts a macro conversion to Phase 4 with the
    learned layer; a number emitted here would be a guess indistinguishable from a derived value once
    it is in the table.

    Assign `seq` to each event as its zero-based index within the (customer, tick) so
    `derive_event_id` produces stable unique ids, and return `sort_events(events)` so `TickResult`
    is already canonically ordered. Iterate customers over a sorted id range, never over a set or a
    dict built from one.

    Guarantee by construction that no emitted event is malformed: prices come from the catalog as
    integer cents, quantities are positive, timestamps come from `SimClock.intraday` with strictly
    increasing offsets inside a session, and every sku referenced exists in the catalog. RESEARCH
    Pitfall 2 makes this explicit — the quarantine table exists for the ingest layer and the future
    public-dataset track, not to give the simulator licence to be sloppy, and plan 01-09 asserts the
    simulator's own reject rate is exactly zero.

    Create `tests/unit/test_tick_loop.py` covering every item in the behavior block against the
    `tiny` profile. Write the stage-ordering test as a direct behavioural comparison: run one tick
    for a fixed customer with an empty queue and once with a queued discount delivery, and assert the
    organic events generated in that same tick differ.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_tick_loop.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_tick_loop.py -x -q` exits 0
    - A test asserts the same-tick organic output differs between an empty queue and a queued delivery for the same customer and tick (proving D-08's ordering)
    - A test asserts every event emitted by a tick validates as a canonical `Event`
    - A test asserts all timestamps within one session strictly increase
    - A test asserts every session emits at least one event of type `scroll`, `filter_apply` or `dwell`
    - `grep -rn 'micro_conversion_weight\|pcr_weight\|micro_weight' src/nextmove | wc -l` outputs `0`
    - A test asserts two runs of the same tick produce identical `event_id` sequences
    - `advance_tick` iterates customers over a sorted range, verified by a test that shuffles the world's customer container and asserts identical output
    - `uv run lint-imports` exits 0
  </acceptance_criteria>
  <done>One tick implementation serves both Phase 1's empty queue and Phase 3's policy replay, emits every SIM-01 and SIM-04 event type, and is deterministic.</done>
</task>

<task type="auto">
  <name>Task 3: Run the full horizon within a stated memory budget, persist raw tables, and prove UC1/UC2 occur</name>
  <files>src/nextmove/simulator/run.py, src/nextmove/simulator/__main__.py, src/nextmove/simulator/__init__.py, justfile, tests/integration/test_uc_scenarios_occur.py, tests/integration/test_simulation_budget.py</files>
  <read_first>
    - `src/nextmove/simulator/tick.py` — written in Task 2
    - `src/nextmove/storage/repository.py` and `src/nextmove/storage/paths.py` — written in plan
      01-06: the exact signatures of `write_table`, `write_table_from_parts`, `write_part_file`,
      `list_part_files` and `clear_staging`; the `root` parameter every one of them accepts; the
      `Zone` and `Stage` members; the fact that staging is **not** a `Zone` and is reachable only
      through `write_part_file`; the `sort_key` tuple-uniqueness precondition; and the rule that only
      the storage package touches Parquet
    - `.planning/phases/01-reproducible-world/01-06-SUMMARY.md` — the as-implemented storage
      signatures, the recorded answer to which functions accept `root`, and the exact `sort_key`
      precondition and its exception message; call them as recorded there, not as sketched here
    - `justfile` — written in plan 01-01 and extended by plan 01-04; this task appends two recipes,
      `simulate` and `budget`, and modifies no existing recipe
    - `config/profiles/default.yaml` — the `n_customers: 50000` and `horizon_days: 548` values this
      task's memory budget has to survive
    - `src/nextmove/config/models.py` and `config/simulator.yaml` — written in plan 01-03; this task
      appends exactly one field, `uplift_snapshot_every_ticks`, to `SimulatorConfig` and its default
      to the YAML, and changes nothing else in either file
    - `src/nextmove/config/loader.py` — `load_config` and `ResolvedConfig` from plan 01-03
    - `.planning/phases/01-reproducible-world/01-CONTEXT.md` — D-05 (UC1 and UC2 must genuinely
      occur in the generated population; these are acceptance targets, not hand-built fixtures) and
      D-18 (canonical tables are Parquet, DVC-tracked, queried directly by DuckDB)
    - `PRODUCT_CHARTER.md` section 4, now at `docs/PRODUCT_CHARTER.md` — the exact UC1 and UC2
      customer profiles the population must contain
    - `.planning/REQUIREMENTS.md` — SIM-02's rule that simulator response functions are never
      visible to models; models see only events
  </read_first>
  <action>
    Add one field to `SimulatorConfig` in `src/nextmove/config/models.py` (created by plan 01-03) and
    its default to `config/simulator.yaml`: `uplift_snapshot_every_ticks: int` constrained greater
    than zero, defaulting to `30`. Add nothing else to either file and change no existing field —
    plan 01-03 owns the rest of that schema, this plan owns only the key it needs. The key belongs in
    config rather than in code because changing it changes the shipped `ground_truth_uplift` table's
    bytes, and ENG-04's lineage row can only account for an output change that is inside the hashed
    config surface; a module constant would let two runs produce different table bytes under an
    identical config hash, which is the same family of false-provenance defect the lineage design
    went to some trouble to close. It is also a simulator parameter, which ENG-03 and plan 01-03's
    "no such number is a literal in `src/nextmove/`" invariant both name explicitly. Because it is
    now a top-level `simulator:` key, plan 01-11's documentation floor check requires it to appear in
    `docs/SIMULATOR_ASSUMPTIONS.md` and fails the build if it does not.

    Create `src/nextmove/simulator/run.py` declaring the module-level constant
    `FLUSH_EVERY_TICKS = 30` and
    `run_simulation(resolved: ResolvedConfig, out_root: Path | None = None,
    flush_every_ticks: int = FLUSH_EVERY_TICKS) -> dict[str, Path]`.
    It initializes the world, constructs an empty `ActionQueue` (Phase 1 runs pure organic history
    per D-07), iterates `world.clock.ticks()` calling `advance_tick`, and writes through the storage
    layer only — never touching Parquet directly, because plan 01-04's import contract forbids it.

    Forward `out_root` to every storage call as the `root` argument — to `write_part_file`, to
    `write_table_from_parts`, to `write_table`, to `list_part_files` and to `clear_staging`. Plan
    01-06 gives all of them a `root` parameter for exactly this reason, and `run_simulation` must not
    accept a root it then drops: plan 01-11's headline golden test runs this profile twice into two
    separate roots and compares every file's sha256, and phase success criterion 1 rests on that
    comparison being a real experiment rather than one run overwriting the other. Return the resolved
    absolute paths of the tables actually written, so the caller can hash what was produced without
    re-deriving the root itself.

    **Bound the peak heap by flushing, not by hoping.** Do not accumulate the whole horizon's events
    in a Python list before writing. At the configured default scale — `n_customers: 50000` over
    `horizon_days: 548` — that is plausibly tens of millions of live Pydantic objects, which breaks
    ENG-08's laptop guarantee, and no test would catch it because every test path uses the `tiny` or
    `demo` profile. Instead: append each tick's events to a buffer, and every `flush_every_ticks`
    ticks write the buffer through `write_part_file(rows, part_dir="events_raw",
    part_index=n, sort_key=CANONICAL_SORT_KEY, root=out_root)`, then clear the buffer. After the
    final tick, flush the remainder, call `list_part_files("events_raw", root=out_root)` to collect
    the parts in ascending index order, merge them into the single sorted `events_raw` table with
    `write_table_from_parts`, and call `clear_staging(root=out_root)` to remove the staging tree.
    `write_table_from_parts` shares `write_parquet_atomic` with `write_table`, so the merged output
    is byte-identical to what a single unflushed write would have produced.

    Use `write_part_file` rather than `write_table` for the flush, and note why in a comment: staging
    is deliberately not a `Zone`, so `write_table` — which resolves every destination through
    `resolve_table_path` against a `Zone` — cannot address it and would raise. `write_part_file` is
    the primitive plan 01-06 declares for precisely this path; it takes no `zone` and no
    `record_lineage`, so a part file cannot acquire a lineage row describing a file that is deleted
    before the run exits.

    `flush_every_ticks` is a function parameter with a module-constant default, deliberately **not**
    a config value: it must not enter the hashed config surface, because two runs that differ only in
    flush cadence must be byte-identical and a config difference would imply a legitimate output
    difference. That reasoning is the exact converse of the uplift-cadence decision above — one knob
    cannot change the output and therefore stays out of the hashed surface, the other does change it
    and therefore must be in. The invariance is asserted by test.

    Write four tables into `Zone.raw` with `producer_stage=Stage.simulate`, each with its declared
    sort key and the resolved config hash: `events_raw` (merged from parts, sorted by
    `CANONICAL_SORT_KEY`); `catalog` sorted by `("sku",)`; `inventory_snapshots` sorted by
    `("tick", "sku")`; `campaigns` sorted by `("campaign_id",)`. The latter three are small and
    bounded by catalog and campaign counts rather than by the horizon, so they are written in one
    call. Pass `input_paths=()` — the simulator is a root producer with no input tables, which is why
    its lineage rows carry an empty input list rather than a null.

    Write two tables into `Zone.ground_truth`, also with `producer_stage=Stage.simulate`, and give
    each an explicitly declared sort key rather than leaving one to be inferred: `customer_traits`
    (customer id plus every latent trait, one row per customer) sorted by `("customer_id",)`, and
    `ground_truth_uplift` (customer id, action type, tick, uplift value) sorted by
    `("customer_id", "action_type", "tick")`. Both keys are unique as tuples over their table's rows,
    which is what plan 01-06's `write_parquet_atomic` precondition requires; `sort_key` is a required
    argument, so a table reaching the storage layer without one would fail at the call site rather
    than degrade quietly.

    Materialize `ground_truth_uplift` on a snapshot cadence read from
    `resolved.config.simulator.uplift_snapshot_every_ticks` rather than at every tick: the per-tick
    cross product at default scale is roughly 219 million rows, which fails ENG-08 on its own. The
    monthly default keeps the seasonal and fatigue variation Phase 4 needs while landing near 7.2
    million rows, and because the cadence is a config value a profile can lower it — for instance to
    1 in `tiny.yaml` — without a code edit, which is phase success criterion 5 applied to this
    parameter. This does not narrow D-03 or MODEL-06 — `ground_truth_uplift` the *function* remains
    callable for any (customer, action, tick) triple without running the simulation, and the table is
    a precomputed convenience over it. Flush this table in parts on the same mechanism as
    `events_raw`, using `part_dir="ground_truth_uplift"`. The cadence is recorded as a flagged
    assumption in this plan's frontmatter and must be raised before phase verification if Phase 4
    needs per-tick density.

    These two tables are simulator internals. Add a module docstring stating that nothing under
    `data/ground_truth/` may ever be read by `features/`, `models/`, `decisions/` or `policies/` —
    SIM-02 bans the import path, and this is the data-path counterpart. Plan 01-10 adds the test that
    enforces it.

    Create `src/nextmove/simulator/__main__.py` exposing a CLI accepting `--profile` (default
    `default`) and `--out` (default `None`, meaning the storage layer's `DATA_ROOT`), which loads the
    config, passes `--out` straight through as `run_simulation`'s `out_root`, and prints one summary
    line naming the profile, config hash, tick count, event count and each written table path. Print
    the absolute resolved paths, so a caller that supplied `--out` can see where the run actually
    landed. `--out` is load-bearing rather than convenience: it is how plan 01-11's golden suite
    obtains two independent output trees from one profile. Take the seed from config, never from a
    CLI flag — D-25 puts seeds inside the hashed surface precisely so a determinism claim is auditable
    from the config hash alone.

    Add a `just simulate profile="default"` recipe to `justfile` invoking the module, and a
    `just budget profile="default"` recipe invoking the on-demand budget check described below.
    Modify no recipe another plan owns.

    Create `tests/integration/test_simulation_budget.py`, which converts ENG-08's laptop guarantee
    from an aspiration into an assertion. Declare the budget as an explicit module-level mapping from
    profile name to `(max_peak_heap_mb, max_wall_clock_s)` — stated numbers a reviewer can argue
    with, not an implicit hope. Cover four things:

    Flush invariance — run the `tiny` profile twice with `flush_every_ticks` set to 1 and to a value
    exceeding the horizon, and assert every produced table's sha256 is equal. This proves the memory
    control is not also a determinism control, which is the failure mode a flush strategy most easily
    introduces.

    Bounded accumulation — run the `demo` profile under `tracemalloc`, and assert the peak traced
    Python heap is under the `demo` budget. Additionally assert the number of `events_raw` part files
    written equals `ceil(horizon_days / flush_every_ticks)`, so a flush that silently stopped
    happening fails loudly rather than merely getting slower. `tracemalloc` measures Python object
    allocation specifically, which is the quantity at risk here — Arrow's buffers are off-heap and
    are bounded separately by the part-file size.

    Sub-linear growth — run the `tiny` and `demo` profiles and assert peak traced heap grows by less
    than the ratio of their customer counts. Accumulating the full horizon would grow linearly with
    population times horizon; a correct flush strategy grows with the flush window only.

    Default-profile budget — a test marked `slow` and additionally gated behind an environment
    variable so neither the default suite nor CI runs it, asserting the `default` profile completes
    within its stated wall-clock and peak-heap budget. `just budget profile="default"` sets that
    variable and runs it. CI cannot afford a 50k-customer 548-day run on every push, but "we never
    actually ran the configuration we ship as the default" is not an acceptable answer either; this
    makes it one command a reviewer or a release check can run. Record the measured numbers in the
    SUMMARY.

    Where a POSIX `resource.getrusage` reading is available, also record process RSS alongside the
    traced heap and report both in the failure message; skip the RSS half with a stated reason on
    platforms that do not provide it, rather than failing the test on the development machine.

    Create `tests/integration/test_uc_scenarios_occur.py`, marked `slow`, running the `demo` profile
    end to end and querying the resulting tables through the storage layer's `query`:

    UC1 — count customers who, at some pre-Christmas tick in the configured peak month, have an
    `add_to_cart` for a sku in the high-seasonality category with no subsequent `order_placed` in
    that session, whose sku inventory at that tick is below the configured scarcity threshold, and
    whose latent `price_sensitivity` is in the lowest configured quantile. Assert the count is
    greater than zero and report it in the failure message.

    UC2 — count customers who have an `order_placed` within the configured recency window of some
    tick and a message-fatigue counter above the configured high-fatigue threshold at that tick.
    Assert the count is greater than zero.

    If either count is zero the test must fail with a message naming which UC is absent and the
    nearest-miss counts for each individual clause, so a failure tells the developer which
    parameter to move. These are D-05's acceptance targets — a hand-built fixture customer would
    satisfy the letter and destroy the point.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_uc_scenarios_occur.py tests/integration/test_simulation_budget.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -m nextmove.simulator --profile tiny` exits 0 and prints one summary line containing the config hash and the event count
    - After that run, `data/raw/events_raw.parquet`, `data/raw/catalog.parquet`, `data/raw/inventory_snapshots.parquet` and `data/raw/campaigns.parquet` all exist
    - After that run, `data/ground_truth/customer_traits.parquet` and `data/ground_truth/ground_truth_uplift.parquet` exist
    - After that run, `data/lineage/simulate.parquet` exists and `data/lineage/ingest.parquet` does not (the simulate stage writes only its own fragment)
    - After that run, `data/_staging/` does not exist — every part file was merged and the staging directory removed by `clear_staging`
    - `uv run python -c "from nextmove.storage import read_lineage; print(sorted(r.table_name for r in read_lineage()))"` lists all six tables and no part file
    - `uv run pytest tests/integration/test_uc_scenarios_occur.py -m slow -q` exits 0 and both UC counts are greater than zero
    - **(MEDIUM-1, flush invariance)** A test asserts two `tiny` runs with `flush_every_ticks` of 1 and of a value exceeding the horizon produce equal sha256 digests for every table
    - **(MEDIUM-1, bounded accumulation)** A test asserts the `demo` run's peak `tracemalloc` heap is under the stated `demo` budget, and that the `events_raw` part-file count equals `ceil(horizon_days / flush_every_ticks)`
    - **(MEDIUM-1, sub-linear growth)** A test asserts peak traced heap grows by less than the `tiny`-to-`demo` customer-count ratio
    - **(MEDIUM-1, default budget)** `tests/integration/test_simulation_budget.py` declares an explicit `(max_peak_heap_mb, max_wall_clock_s)` pair for each of `tiny`, `demo`, `ci` and `default`, and `just budget profile="default"` runs the `default` assertion to completion
    - **(HIGH-5, the flush destination is reachable)** `grep -c 'write_part_file' src/nextmove/simulator/run.py` is at least 1, and a `tiny` run completes without raising — the flush no longer routes a staging path through a zone resolver that cannot accept it
    - **(HIGH-5)** A test asserts that during a `tiny` run `data/_staging/events_raw/` and `data/_staging/ground_truth_uplift/` both contain part files mid-run and that neither directory exists after the run returns
    - **(HIGH-5)** A test asserts no lineage row exists for any part file: `read_lineage()` returns exactly the six table names and nothing whose name starts with `part-`
    - **(HIGH-6, out_root reaches disk)** A test calls `run_simulation` twice with two `tmp_path` roots for the same profile and asserts every produced file's sha256 is equal across the two roots, that each root holds its own `raw/`, `ground_truth/` and `lineage/simulate.parquet`, and that the repository's own `data/` directory gained nothing
    - **(HIGH-6)** `uv run python -m nextmove.simulator --profile tiny --out <tmpdir>` exits 0 and writes under `<tmpdir>` only, asserted by a test
    - **(HIGH-8, every table declares a unique-tuple sort key)** A test asserts all six `write_table` / `write_table_from_parts` calls in `run.py` pass a `sort_key`, and asserts the two ground-truth tables use `("customer_id",)` and `("customer_id", "action_type", "tick")` respectively
    - **(MEDIUM-3, cadence is config)** `grep -c 'uplift_snapshot_every_ticks' config/simulator.yaml` is at least 1 and `grep -c 'uplift_snapshot_every_ticks' src/nextmove/simulator/run.py` is at least 1
    - **(MEDIUM-3)** A test imports `nextmove.simulator.run`, collects every module-level `int` attribute whose name ends in `_EVERY_TICKS`, and asserts the set of their names is exactly `{"FLUSH_EVERY_TICKS"}` — the uplift cadence is no longer a module constant
    - **(MEDIUM-3)** A test loads the `tiny` profile, overrides `simulator.uplift_snapshot_every_ticks` in a temporary config tree, and asserts both the config hash and the `ground_truth_uplift` row count change — proving the cadence is inside the hashed surface and steers behaviour from YAML per phase success criterion 5
    - `src/nextmove/simulator/__main__.py` accepts no seed argument: `grep -Ec '\-\-seed|add_argument[^)]*seed' src/nextmove/simulator/__main__.py` outputs `0`, and a test asserts the parsed argument namespace contains no attribute whose name contains `seed`. The gate targets argument declarations rather than the bare token, so the docstring explaining why seeds come from config (D-25) cannot trip it
    - `just simulate profile="tiny"` exits 0 and `just budget profile="tiny"` exits 0
    - `uv run lint-imports` exits 0
    - The UC test queries the generated population and does not construct any customer by hand
  </acceptance_criteria>
  <done>A full-horizon seeded run writes six raw tables through the storage layer inside a stated and asserted memory budget, the flush cadence provably cannot change the output bytes, and the UC1 and UC2 profiles are proven to occur naturally.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (plan 01-08)

**New files:** `src/nextmove/simulator/response.py`, `src/nextmove/simulator/queue.py`,
`src/nextmove/simulator/tick.py`, `src/nextmove/simulator/run.py`,
`src/nextmove/simulator/__main__.py`, `tests/unit/test_response_functions.py`,
`tests/unit/test_tick_loop.py`, `tests/integration/test_uc_scenarios_occur.py`,
`tests/integration/test_simulation_budget.py`.

**Modified files:** `justfile` (created by plan 01-01); `src/nextmove/config/models.py` and
`config/simulator.yaml` (both created by plan 01-03, in an earlier wave) — each gains exactly one
new field or key and nothing else.

**New classes / enums:** `ActionType` (members `none`, `wait`, `recommend`, `bundle`,
`discount_low`, `discount_high`, `email_now`, `email_delayed`), `ScheduledAction`, `ActionQueue`,
`TickResult`.

**New functions:** `apply_discount_cents`, `fatigue_penalty`, `response_multiplier`,
`base_conversion_probability`, `ground_truth_uplift`, `advance_tick`, `run_simulation`,
`ActionQueue.schedule`, `ActionQueue.drain`, `ActionQueue.pending_count`.

**New module constants:** `FLUSH_EVERY_TICKS` (default 30) in `src/nextmove/simulator/run.py` — the
only `_EVERY_TICKS` constant in that module.

**New config field (appended to plan 01-03's `SimulatorConfig` in
`src/nextmove/config/models.py`):** `uplift_snapshot_every_ticks: int`, greater than zero, default
`30`. **New config key (appended to `config/simulator.yaml` under `simulator:`):**
`uplift_snapshot_every_ticks`. It is a top-level `simulator:` key, so plan 01-11's documentation
floor check requires it to appear in `docs/SIMULATOR_ASSUMPTIONS.md`.

**New function parameter:** `run_simulation(..., flush_every_ticks: int = FLUSH_EVERY_TICKS)` —
deliberately a parameter with a module-constant default rather than a config key, because it cannot
change the output bytes. The uplift cadence is the converse case and is therefore config.

**Storage calls this plan makes (plan 01-06's surface):** `write_part_file` for every flush,
`list_part_files` to collect them, `write_table_from_parts` to merge, `write_table` for the four
small tables, `clear_staging` to remove the staging tree — every one of them passed
`root=out_root`.

**New CLI:** `python -m nextmove.simulator` with flags `--profile` and `--out`, where `--out`
becomes `run_simulation`'s `out_root` and reaches disk through the storage layer's `root`.

**New `just` recipes:** `simulate`, `budget`.

**New tables in `data/raw/`:** `events_raw` (sort key `CANONICAL_SORT_KEY`), `catalog`
(`("sku",)`), `inventory_snapshots` (`("tick", "sku")`), `campaigns` (`("campaign_id",)`).

**New tables in `data/ground_truth/`:** `customer_traits` (sort key `("customer_id",)`),
`ground_truth_uplift` (sort key `("customer_id", "action_type", "tick")`, materialized on the
`simulator.uplift_snapshot_every_ticks` cadence, not per tick).

**New lineage fragment written:** `data/lineage/simulate.parquet` (plan 01-06's `Stage.simulate`).

**New transient staging paths, deleted before the run exits:** `data/_staging/events_raw/` and
`data/_staging/ground_truth_uplift/`, holding zero-padded numbered flush part files written by
`write_part_file`. Neither is a `Zone`, neither carries a lineage row, and both are removed by
`clear_staging`.

**New documented assumptions (docstrings lifted to `SIMULATOR_ASSUMPTIONS.md` by plan 01-11):** the
`response_multiplier` combining formula, the fatigue-penalty form and its config toggle (D-04
loophole), the base conversion probability form, micro-event emission rates and the dwell-class
boundaries.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| simulator internals to downstream data | Ground-truth traits and uplift are written to disk; the import ban stops code from reaching them, but the data path needs its own discipline |
| simulator output to every later claim | Every result the project publishes traces back to these tables; a silent generation bug is indistinguishable from a real finding |
| shipped default configuration to the reviewer's laptop | The published claim is that one command reproduces the full history on a laptop; a default profile that exhausts memory converts that claim into a false statement, and every test path uses a smaller profile |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-19 | Information Disclosure | `data/ground_truth/` tables readable by feature code | high | mitigate | Ground-truth tables are written to their own `Zone.ground_truth` directory, `run.py` documents the prohibition, and plan 01-10 adds a test asserting no feature definition or transform reads from that zone. This is the data-path counterpart to SIM-02's import ban; without it, models could see simulator answers while passing the import contract. |
| T-01-20 | Tampering | The D-04 fatigue-penalty toggle mistaken for a bug or silently flipped | medium | mitigate | The toggle lives in `config/simulator.yaml` under an explicitly named `loophole` block, defaults to enabled, and `fatigue_penalty`'s docstring states its purpose; plan 01-11 documents it in `SIMULATOR_ASSUMPTIONS.md`. A loophole that is not written down is just a bug. |
| T-01-34 | Denial of Service | Full-scale run exhausting laptop memory, making the shipped default configuration unrunnable | high | mitigate | Events and uplift snapshots are flushed every `FLUSH_EVERY_TICKS` ticks through `write_part_file` — plan 01-06's staging primitive, which resolves through `resolve_staging_path` and therefore has a destination the storage API can actually address; the earlier design routed the flush through `write_table`, whose zone resolver cannot reach the staging root, so the flush would have raised on its first call and this mitigation would have been void (review HIGH-5). Parts are collected by `list_part_files`, merged by `write_table_from_parts` and removed by `clear_staging`, so peak Python heap tracks the flush window rather than the horizon. `ground_truth_uplift` is materialized on the `simulator.uplift_snapshot_every_ticks` cadence rather than per tick, avoiding a roughly 219-million-row cross product at default scale. Enforced by `tests/integration/test_simulation_budget.py`: an explicit per-profile heap and wall-clock budget, a part-file count assertion so a silently-disabled flush fails loudly, a sub-linear growth assertion across two profiles, and an on-demand `default`-profile check behind `just budget`. Residual, accepted: CI does not run the `default` budget on every push, so a regression there is caught by the release check rather than the build. |
| T-01-35 | Tampering | Flush cadence silently changing output bytes and invalidating the determinism claim | high | mitigate | `flush_every_ticks` is a function parameter with a module-constant default, deliberately outside the hashed config surface, and the merge path shares `write_parquet_atomic` with the single-call path so the sort and writer options are identical. A test runs the same profile with a flush interval of 1 and one exceeding the horizon and asserts every table's sha256 is equal — without it, a memory fix would quietly become a reproducibility defect. |
| T-01-21 | Tampering | Malformed events generated by the simulator itself | medium | mitigate | Events are constructed through the validated `Event` contract at emission, prices come from the catalog as integers, session timestamps are strictly increasing by construction, and every sku is catalog-resolved. Plan 01-09 asserts the simulator's own reject rate is exactly zero on a seeded run, which converts "should not happen" into a gate. |
| T-01-36 | Repudiation | An output-determining parameter living outside the hashed config surface, so a lineage row asserts a reproducibility property it no longer has | high | mitigate | The ground-truth uplift snapshot cadence materially determines the shipped `ground_truth_uplift` table's contents, so it lives in `SimulatorConfig` and `config/simulator.yaml` and enters the config hash — two runs with different cadences therefore carry different config hashes and their lineage rows stay truthful. The converse case is separated and reasoned rather than conflated: `flush_every_ticks` cannot change the output bytes and is therefore deliberately outside the hashed surface, which a flush-invariance test proves. Enforced by a test asserting the only `_EVERY_TICKS` module constant in `run.py` is `FLUSH_EVERY_TICKS`, and by a test asserting that changing the cadence in YAML changes both the config hash and the table's row count (review MEDIUM-3). |
</threat_model>

<verification>
- One tick executes queue drain, response application, then organic generation, proven by a same-tick behavioural difference
- Every emitted record validates as a canonical `Event`; session timestamps strictly increase
- Micro-conversion events are emitted with no attached weights
- The fatigue loophole is toggleable in config and documented in code
- `ground_truth_uplift` is computable per (customer, action) pair without running the simulation
- A `demo`-profile run contains at least one UC1 and one UC2 customer, found by query
- Six tables land in `data/raw/` and `data/ground_truth/`, each with an explicitly declared unique-tuple sort key, with lineage rows in `data/lineage/simulate.parquet` and no other fragment touched
- Peak Python heap is bounded by the flush window, asserted per profile, and the staging directory is gone when the run exits
- The flush lands through `write_part_file` and therefore actually executes; no part file acquires a lineage row
- Two runs differing only in flush cadence produce byte-identical tables
- Two runs into two `--out` roots produce equal digests and neither writes into the other's tree
- The uplift snapshot cadence is a YAML value; changing it changes both the config hash and the table
- The `default` profile's stated budget is runnable on demand via `just budget`
</verification>

<success_criteria>
- SIM-01 satisfied: a responding mid-market e-commerce world with explicit, documented action-response functions
- SIM-04 satisfied: scroll, filter and dwell-class micro-events emitted alongside every macro event
- D-05 satisfied: UC1 and UC2 genuinely occur in the generated population
- D-07 satisfied: one tick code path, run here with an empty queue, reusable by Phase 3 unchanged
</success_criteria>

<output>
Create `.planning/phases/01-reproducible-world/01-08-SUMMARY.md` when done. Reproduce the
`response_multiplier` formula, the fatigue-penalty form, the micro-event rates and the dwell-class
boundaries verbatim — plan 01-11 must document every one of them in `docs/SIMULATOR_ASSUMPTIONS.md`
per SIM-03. Record the measured peak heap and wall-clock for every profile the budget suite ran,
including the `default` profile if `just budget profile="default"` was executed, so ENG-08's laptop
claim rests on a number rather than an assurance. Record the six tables with the exact `sort_key`
tuple each was written with, since plan 01-06's precondition is checked against them. Record the
resolved value and YAML location of `simulator.uplift_snapshot_every_ticks` and the measured
`ground_truth_uplift` row count at each profile, because plan 01-11 must document the cadence in
`docs/SIMULATOR_ASSUMPTIONS.md`. Restate both flagged assumptions from this plan's frontmatter — the
SIM-04 micro-event fidelity question and the `ground_truth_uplift` snapshot cadence — so both reach
phase verification.
</output>
