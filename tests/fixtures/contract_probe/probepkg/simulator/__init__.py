"""Probe stand-in for `nextmove.simulator`. Trivial and never imported by production code."""


def trivial() -> int:
    """Return a constant. Exists only so this module is more than an empty docstring."""
    return 1
